# ADM 一期产品需求文档

> 版本：V0.17  
> 状态：完整评审稿  
> 最近更新：2026-08-28  
> 适用范围：ADM 解析记录、ADM 业务单、OTA 追偿记录，以及与既有供应商内部账单/OTA 通用账单能力的衔接

## 历史记录

| 版本 | 日期 | 修改人 | 主要变更 |
|---|---|---|---|
| V0.17 | 2026-08-28 | AI 协作整理 | 恢复已确认的“先关联、后核实”主流程：业务单生成后系统立即查询并关联内部订单/外部来源，人工基于系统证据核实 ADM 事件；事件结论修正不再清空仍有效的关联事实。详情页将“订单关联”与“来源线索与 OTA 责任”合并为单一证据与责任板块，来源与责任仍严格分离 |
| V0.16 | 2026-08-28 | AI 协作整理 | 完成 1440px 静态视觉审查与回归修复：列表类型/OTA 标签禁止逐字换行，详情头状态标签顶对齐，账单前置条件改用明确下一步文案映射，消除“核实核实”等机械拼接；不改变业务规则与范围 |
| V0.15 | 2026-08-27 | AI 协作整理 | 根据上下文隔离独立验收补齐：全字段均来自系统硬性规则时由系统自动确认并生成/关联业务单，否则必须人工二次确认；币种枚举直读“汇率管理”去重后补充 CNY；事件成立前禁止选择 ADM 类型；人工状态流转统一复用 StateTransition，原型标注统一复用 PrototypeNote；补齐供应商多邮件模板组合预览/轮次留痕、三模块精确更多筛选及日期格式回归 |
| V0.14 | 2026-08-27 | AI 协作整理 | 适配最新版 AGENTS V2.2、DESIGN V1.13、PRD 模板 V1.15 与原型模板 V0.5；综合 2025-11 至 2026-02 四个月月报重排高频场景；补充退款差错、佣金差错、多票号、无票号与多币种真实 PDF 校验；所有业务流程改为结构化文字流程图；列表页统一改为经典“搜索区 + 数据区 + 独立详情页”结构，并收敛业务页面上的产品设计说明 |
| V0.13 | 2026-08-27 | AI 协作整理 | 根据独立验收修复数据完整性与状态可达性：ADM 单号规范键防空白变体重复；最终关联增加具体目标有效性核验；统一五项金额修改与追偿联动；上游修正后旧账单必须完成差异复核；补齐业务单/追偿单回退、终止、重开路径；明确已完成追偿被责任修正时保留历史终态、仅解除当前关系；完善解析筛选、逐项 PNR/票号、日期留痕和跨模块定向跳转 |
| V0.12 | 2026-08-27 | AI 协作整理 | 收紧事件前置校验：只有人工确认 ADM 事件成立，才可进入下游关联、OTA 判责、供应商内部账单与有效业务单完成；事件不成立时仅允许申诉/补证，供应商最终撤销且金额归零后以“无需继续处理”结束；进一步明确外部提交金额属于操作审计，不是金额版本快照 |
| V0.11 | 2026-08-26 | AI 协作整理 | 按用户最新事实与结论重构状态、责任、金额和账单方案；新增“待材料补充”；明确 OTA 追偿由人工提交；增加四家授权 OTA 的 PNR 来源特殊分支；纳入 2026 年 2 月月报和 BSP/ARC 真实 ADM 样例，将重复预订、无效航段未擦除、更正票价列为一期高频优化重点 |
| V0.10 | 2026-08-24 | AI 协作整理 | 补充 ADM 单号唯一、订单/来源关联状态及多 PNR/票号规则 |
| V0.7-V0.9 | 2026-08-19 至 2026-08-23 | AI 协作整理 | 形成 ADM 解析、业务单、供应商申诉、OTA 追偿及账单衔接方案 |

## 输入资料与确认依据

### 1. 资料优先级

1. 当前对话中用户明确为“事实”的内容不可推翻。
2. 当前对话中用户明确为“结论”的内容作为当前方案基线；后续可由新的业务结论调整。
3. 项目 `AGENTS.md`、`PRODUCT.md`、`DESIGN.md` 和通用需求模板约束产物结构与设计方法。
4. 已确认项目资料用于补充细节；与当前对话冲突时，以当前对话为准并记录冲突。
5. 月报与真实 ADM 文件用于验证量级、场景、字段和版式差异，不单独创造业务规则。
6. 测试一下
7. 测试二

### 2. 来源清单

| 编号 | 来源 | 用途 | 采用结论 |
|---|---|---|---|
| SRC-001 | 当前对话，2026-08-28 | 最新业务事实与产品结论 | 作为本版最高优先级业务输入；事实不可推翻，产品结论按当前阶段执行 |
| SRC-002 | `AGENTS.md` V2.2 | 项目协作规范 | 完整 PRD 与完整原型同步修改；原文件原位更新；产品阶段不新增或修改 AC |
| SRC-003 | `PRODUCT.md` V1.11 | 产品规范 | 状态表达当前进度、责任角色和下一步动作；状态矩阵、流转明细和状态定义齐备 |
| SRC-004 | `DESIGN.md` V1.13 | 原型规范 | 复用成熟交互；高风险金额操作可追溯；真实数据优先；技术异常不无必要阻断正常业务 |
| SRC-005 | `通用需求模板-AI协作版.md` V1.15、`原型设计模板.html` V0.5 | 文档与原型结构 | 业务流程使用结构化文字流程图；状态三表齐备；状态流转与原型标注复用稳定实现 |
| SRC-006 | `adm业务单和账单生成流程（亲写已确认，重点）.md` | 业务主流程 | ADM 事实、金额、供应商处理和账单衔接基础 |
| SRC-007 | `ADM一期已确认规则与关键决策.md` | 已确认规则 | 解析、人工核实、配置和既有能力复用 |
| SRC-008 | `adm需求调研会议纪要（已确认）.md` | 调研事实 | 角色、现状、业务场景和历史痛点 |
| SRC-009 | 2025 年 11 月至 2026 年 2 月四份 ADM 月报 | 高频场景与业务量级 | 四个月共处理 3,725 单；重复预订每月持续高发，共 991 单；售后问题按部门维度共 801 单；航段取消率在 11 月至 1 月报告 748 单；无效航段未擦除在 12 月至 2 月至少 469 单；更正票价 2 月 31 单但金额 3.8W。月报属于业务快照，不作为系统成果或长期目标值 |
| SRC-010 | `ADM-PDF例子` 中 BSP/ARC 真实文件及解析说明 | 字段、格式与场景校验 | 覆盖多 PNR、无票号、虚拟票号、单票号、多票号、退款差错、佣金差错、Churning、Inactive/Duplicate Segment、多币种、附件和金额构成；验证数组字段、原文保留与场景卡输入 |
| SRC-011 | `adm_6606501569.pdf`、`adm_6123211440.pdf`、`adm_6865650140.pdf`、`adm_6112602828.pdf`、`adm_6112602837.pdf`、`ARC ADM 8966533300.pdf`、`8960041504.pdf` 等代表性样本 | 原型演示数据与边界验证 | 代表 BSP/ARC、原因文本多 PNR、虚拟票号、无票号、多票号、退款罚金、佣金差异、航司管理费和不同币种；样本分布偏重复预订，不据此推断全量类型占比 |

### 3. 资料冲突与最终采用依据

| 冲突编号 | 主题 | 原资料/旧方案 | 本版采用方案 | 采用依据 |
|---|---|---|---|---|
| C-001 | PNR 来源与 OTA 责任 | 旧方案容易把系统识别到的 OTA 来源直接视为 OTA 责任 | 来源只作为系统线索；OTA 责任必须由人工核实，未核实前有效业务单不能完成 | SRC-001 事实与结论 |
| C-002 | OTA 追偿初始状态 | 旧方案为“待提交追偿” | 系统创建记录后先进入“待材料补充”；人工确认材料完整后才进入“待提交追偿”，外部提交仍由人工执行 | SRC-001 结论 |
| C-003 | 状态与结果 | 旧方案/月报中存在“申诉成功、部分成功、申诉失败”等状态式表达 | 成功、部分成功、失败只作为结果字段和统计维度；状态只表达下一步处理 | SRC-001、SRC-003 |
| C-004 | 技术异常状态 | 旧方案曾讨论“GDS 提取失败、提交失败”等状态 | 一期不增加技术异常状态；自动重试，仍失败则保留原业务状态并通知人工、触发技术告警 | SRC-001、SRC-004 |
| C-005 | ADM 内部账单 | 旧文档表达为独立“ADM 内部账单”能力 | 生成到既有供应商内部账单模块，业务类型为“ADM”；后续全部走通用流程 | SRC-001 事实 |
| C-006 | 金额历史 | 旧方案包含金额凭证、快照或多版本倾向 | 每个金额语义字段只保存当前最新值；修改历史进入统一操作记录，一期不做金额快照表 | SRC-001 结论 |
| C-007 | 月报申诉口径 | 月报按“待申诉、申诉中、申诉成功、申诉失败”统计 | 报表可继续按进度状态与结果字段组合统计，但业务对象状态不使用结果型名称 | SRC-001、SRC-009 |
| C-008 | 解析记录状态 | 已确认旧资料使用“待ADM处理员处理/已生成业务单/已作废”，其中后两项同时表达处理结果 | 按最新状态原则改为“待人工确认/处理结束”，另设“生成新业务单/关联已有业务单/文件无需处理”结果字段；处理结束支持授权重开 | SRC-001 状态原则优先；通过授权重开保留旧方案的纠错能力 |
| C-009 | 责任核实与供应商账单时序 | 旧草案把 OTA 责任与追偿记录作为进入供应商账单的前置条件 | 供应商账单条件满足即可生成；责任未核实或追偿记录未创建只阻断业务单“处理完成” | SRC-001 只明确“有效业务单不能完成”；SRC-006/SRC-007 的既有账单复用边界 |
| C-010 | 最终关联目标数量 | 旧草案假设整单只能关联唯一一个内部订单或唯一一个 OTA | 状态仍只保留三类；同一类别下允许一个或多个具体目标，内部/外部混合归属仍待确认 | SRC-001 明确状态与“具体订单/OTA”，但未规定只能一个；多 PNR/票号事实要求避免无依据收窄 |
| C-011 | 已完成追偿的责任修正 | 旧规则只说明“原活跃追偿进入无需继续处理”，未覆盖已完成记录 | 活跃记录进入“无需继续处理”；已完成记录不改写原状态和结果，只标记不再是当前责任关系，并为新责任 OTA 创建新记录 | 状态只表达真实处理进度；不得把已发生结果改写成另一终态；见 A-007/Q-007 |
| C-012 | 高频场景优先级 | 旧版只依据 2026 年 2 月，把重复预订、无效航段未擦除、更正票价作为前三类重点 | 跨月高频优先覆盖重复预订、航段取消率、无效航段未擦除和售后退票/改签；更正票价保留为金额高风险场景 | SRC-009；同一报表中的 ADM 类型维度与部门维度分开使用，不相加计算覆盖率 |
| C-013 | 列表页结构 | 旧原型使用左侧卡片列表 + 右侧详情的主从工作台 | 三个列表页统一采用独立搜索区 + 表格数据区 + 分页/批量基础区；详情通过行操作进入，返回保留筛选和定位 | 当前对话明确要求“传统经典页面结构” |
| C-014 | 业务页面说明文案 | 旧原型在页面标题、卡片和弹窗中直接展示“状态/结果分离、不建设快照”等产品设计说明 | 业务用户真正需要的规则保留为操作提示；仅用于评审的解释改用 PrototypeNote，不占用业务页面 | DESIGN V1.13、原型模板 V0.5；业务页面只表达业务任务 |
| C-015 | 解析确认方式 | V0.14 将所有可用解析结果都表达为“等待人工确认” | 仅当所有生成/关联业务单必需字段均由系统硬性规则唯一确定、无候选冲突/人工修正且全部校验通过时，由系统自动确认；其他任何情况必须人工二次确认 | SRC-006/SRC-007 已确认规则优先；独立验收发现 V0.14 漏项 |
| C-016 | 币种输入来源 | 旧原型允许手输任意三位币种，旧文档只写“未知不得默认” | 币种只能从直连系统“汇率管理”币种去重结果中选择，并额外补充 CNY；不得以任意三位文本绕过字典 | SRC-006/SRC-007 已确认规则；独立验收发现交互未落地 |
| C-017 | 订单/来源关联与事件核实时序 | V0.12-V0.16 将“事件成立”错误扩大为关联前置条件，导致系统证据必须等人工判断后才能取得 | 恢复 SRC-006 已确认主流程：业务单生成后系统先自动查询/关联内部订单与外部来源线索，再由 ADM 处理员基于这些证据核实事件；关联事实不是事件结论的下游结果 | 当前对话 2026-08-28 明确确认；SRC-006 亲写已确认主流程 |

### 4. 当前对话事实/结论落地索引

| 编号 | 当前事实/结论 | 本版落地位置 | 验证方式 |
|---|---|---|---|
| D-001 | 系统标记 PNR 操作来源；OTA 外部追偿由人工提交 | 核心子流程 2、模块 2 第 6 阶段、模块 3 第 2 阶段 | 来源识别只写关联/证据；追偿“已提交”必须登记人工渠道与时间 |
| D-002 | OTA 责任未核实时，有效 ADM 业务单不能完成 | 模块 2 完成校验、A 状态矩阵、BR-007 | `处理完成`校验必须出现“责任已核实” |
| D-003 | OTA 责任成立后系统自动创建追偿记录，初始待材料补充 | 模块 2 第 6 阶段、模块 3 创建条件和 T0 | 创建成功前阻断业务单完成；不自动外部提交 |
| D-004 | 状态表达进度/角色/下一步；结果和技术异常不占用业务状态 | 三模块状态矩阵、结果字段、技术异常处理 | 状态枚举中无“申诉成功/失败、提交失败、GDS 提取失败” |
| D-005 | 金额只保留当前值，历史通过操作记录追溯 | 模块 2 金额字段与操作记录、模块 3 金额字段 | 修改必须记录前值、后值、操作人、时间和必要原因；无金额快照对象 |
| D-006 | ADM 内部账单复用既有供应商内部账单模块 | 模块 2 第 7 阶段、接口与复用能力 | 业务类型写入 ADM；调整/冲销/核销走通用流程 |
| D-007 | 四家授权 OTA 是 PNR 来源识别的限定特殊分支 | 核心子流程 2、BR-005 | 未确认进入特殊分支时禁用“无三家标识即淘宝” |
| D-008 | ADM 单号唯一对应一张业务单，可含多个 PNR/票号 | 核心对象、模块 1 去重与数组字段 | 规范键唯一校验；PNR/票号按 0..N 明细保存 |
| D-009 | 业务单生成后先由系统完成可自动执行的订单/来源关联，再由人工核实 ADM 事件 | 核心主流程第 2-3 阶段、模块 2 第 1-2 阶段、BR-003 | 事件核实前允许并优先执行 OMS/直连/GDS 查询与确定性关联；事件未成立/待补充时只阻断类型、最终金额、责任、账单和完成，不清空仍有效的关联事实 |
| D-010 | 最终关联状态仅三类，最终态必须带具体且有效目标 | 模块 2 关联流程、R 状态矩阵、BR-004 | 待确认持续生成待办并阻断完成；内部/外部分别校验订单号/OTA |
| D-011 | 列表页采用搜索区 + 表格数据区 + 分页，详情独立进入 | 三模块页面与交互 | 详情返回保留筛选、页码、排序与原行定位 |

## 一、项目概要

### 1. 项目背景

ADM 来自 BSP、ARC 等外部渠道，文件结构不统一，业务人员需要人工识别 ADM 单号、航司/代理、PNR/票号、原因、币种、金额和附件，并进一步核实事件是否成立、订单或外部来源、最终金额、供应商申诉和 OTA 责任。现状中信息分散、重复录入、状态口径混乱，且容易将“系统识别到来源”误解为“已经确认责任”。

真实 ADM 样本同时证明：一张 ADM 可能没有真实票号，也可能包含一个或多个 PNR、一个或多个真实/虚拟票号；关键信息可能位于结构化字段、原因长文本或附件中；BSP 与 ARC 的字段名称、金额构成和页面长度差异明显。因此一期不能只做“读取 PDF 首个票号并关联订单”，而必须保留原文、数组明细、字段置信与人工核实边界。

#### 1.1 问题价值与高频依据

| 分析维度 | 月报事实 | 产品判断 | 本期处理 |
|---|---|---|---|
| ADM 类型：重复预订 | 2025-11 至 2026-02 分别为 107、175、515、194 单，共 991 单 | 跨月持续高发，且真实样本大量出现多 PNR、虚拟票号和原因长文本 | P0 场景卡；批量 PNR 查询、逐项来源线索、取消次数/航段集中展示 |
| ADM 类型：航段取消率 | 2025-11 至 2026-01 月报分别报告 30、359、359 单；2 月未单列数量 | 高频但 12 月与 1 月数值完全相同，原始明细未核验；只作为方向性证据 | P0 场景卡；集中展示航线/航司/采购地、取消率依据和内部设置/业务补充入口 |
| ADM 类型：无效航段未擦除 | 2025-12 至 2026-02 分别为 121、240、108 单；11 月已列为增幅来源但未给数量 | 跨月持续出现，HX、UN/TK、未出票和航变为主要核实要素 | P0 场景卡；按 PNR/航段展示状态时间、清段记录、航变与出票事实 |
| 部门维度：售后问题 | 四个月分别为 197、193、229、182 单，共 801 单；报告多次提到退票税费多退、退款罚金和改签 TC 码 | 属于高频部门维度，不与 ADM 类型数量相加；需要复用通用流程并缩短退改事实核实 | P0 售后核实卡；展示退款/改签前后金额、税费/罚金/TC 码与操作记录 |
| ADM 类型：更正票价 | 2026-02 为 31 单、金额 3.8W | 频率低于前述场景，但单类金额风险高 | P1 金额风险卡；并列展示出票价、保证运价/条款、航司差额和原始费用构成 |
| 返点、虚假预订及其他 | 四个月返点报告共 132 单；2 月虚假预订 18 单 | 仍需可检索和通用核实，但当前证据不足以支持独立状态或复杂专用流程 | P2 通用卡 + 快捷筛选；后续按上线数据决定是否专门优化 |

> 月报中的“ADM 类型、部门、平台、航司”属于不同分析维度，不能简单相加为覆盖率。上述数据只用于判断优先级；取消率的重复数值、各月分类口径与原始明细仍需在上线前核验。

#### 1.2 根因判断

- **当前判断：** 核心问题不是缺少更多状态，而是原始证据分散、事件/来源/责任/结果混为一体、人工在高频场景中反复跨系统查找，以及供应商账单和 OTA 追偿边界不清。
- **关键约束：** 事件与责任不能可靠自动判断；系统可提供解析、确定性来源线索、内部订单查询和金额计算候选。仅“全部生成/关联必需字段均由系统硬性规则唯一确定”的解析记录可由系统自动确认；事件、责任和外部反馈结果始终必须由人工确认。
- **证据缺口：** 月报分类明细、处理时长基线、平台规则主数据和部分场景的标准输入尚未结构化；不影响建立通用闭环，但影响自动计算与目标值。
- **结论状态：** 通用闭环与本版最新事实/结论已确认；高频排序基于四个月月报，属于当前产品判断，待上线数据持续校准。

### 2. 项目目标

#### 2.1 业务目标

- 建立 ADM 单号到 ADM 业务单的一对一关系，允许一张业务单承载多个 PNR 和多个票号。
- ADM 业务单生成后先由系统完成可自动执行的订单/来源查询与确定性关联，再由 ADM 处理员基于已准备的证据核实事件；事件未成立前不形成类型、最终金额、责任、账单等业务结论。
- 将订单/来源最终关联严格收敛为三类：待确认、已关联内部订单、已关联外部来源。
- 将“PNR 操作来源线索”和“OTA 责任结论”彻底分开：系统标记来源，人工核实责任。
- 对确认 OTA 责任的业务单自动创建 OTA 追偿记录，但由人工补材料并完成人工外部提交。
- 将供应商内部账单生成纳入既有供应商内部账单模块，并继续走通用账单流程。
- 优先降低重复预订、航段取消率、无效航段未擦除和售后退改场景的查找、录入与核实成本，并对更正票价保持金额风险强化。

#### 2.2 产品原则

- **证据先准备、事实再判断：** 业务单生成后先执行系统可可靠完成的订单/来源查询；ADM 事件核实结果明确前，不进入类型、最终金额、责任和账单完成环节。
- **状态与结果分离：** 状态回答“现在轮到谁做什么”；业务结果由独立字段表达。
- **系统提供证据，人工形成判断：** 自动解析、映射、查询和确定性计算可由系统完成；全字段硬规则解析可系统确认，其他解析必须人工二次确认；事件、责任和外部反馈结果始终由人工确认。
- **高频场景不拆状态机：** 高频差异通过核实卡、筛选、默认展开项和材料模板加速，不为每个 ADM 类型新建状态。
- **当前值 + 操作记录：** 金额字段只保留当前值，变更统一留痕。
- **复用已有能力：** 邮件只复用既有发件能力；账单只复用现有通用模块；不新建邮件会话和独立 ADM 账单系统。

### 3. 范围

#### 3.1 一期范围

| 模块 | 一期能力 |
|---|---|
| ADM 解析记录 | 上传/接收真实 ADM；BSP/ARC 等格式解析；字段校验；ADM 单号去重；多 PNR/票号提取；人工补录；生成唯一业务单 |
| ADM 业务单 | 事件核实、订单/来源关联、场景化核实、金额核实、内部业务补充、供应商申诉、OTA 责任核实、生成供应商内部账单 |
| OTA 追偿记录 | 自动创建、待材料补充、人工提交、记录 OTA 反馈、确认追偿结果、衔接既有 OTA 通用账单 |
| 高频场景优化 | 重复预订、航段取消率、无效航段未擦除、售后退票/改签重点优化；更正票价作为金额高风险场景强化；返点、虚假预订提供筛选与通用场景卡 |
| 审计与提醒 | 状态、结果、金额变更、外部提交、账单和自动异常统一操作记录；待办与技术告警 |

#### 3.2 一期不做

- 不自动判断 ADM 事件成立与否。
- 不根据 PNR 来源自动认定 OTA 责任。
- 不由系统自动向 OTA 外部渠道提交追偿。
- 不建设供应商邮件收件、回复和会话管理。
- 不为 ADM 新建独立供应商内部账单流程。
- 不建设金额快照、金额版本表或明细级金额分摊。
- 不按每个 ADM 类型拆分状态机。
- 不处理一张 ADM 在多个内部订单或多个 OTA 之间自动拆单、拆责和拆金额。
- 不新增“GDS 提取失败”“创建追偿失败”“账单生成失败”等技术状态。

### 4. 角色与权限

| 角色 | 主要职责 | 关键权限 |
|---|---|---|
| ADM 处理员 | 解析确认、事件核实、归属确认、金额核实、供应商申诉、OTA 责任核实、追偿跟进 | 新建/处理业务单；修改未入账的当前金额；发起业务补充；确认责任和结果；生成人工外部提交记录 |
| 出票/退票/商务 | 回答事实、政策、运价、退款或操作问题并补充材料 | 查看分配给本人的核实任务；提交说明和附件；不可直接修改 ADM 最终结论 |
| 财务/账单角色 | 在既有模块处理生成后的供应商内部账单和 OTA 通用账单 | 按既有账单权限处理；不在 ADM 页面改写已入账金额 |
| 技术支持 | 接收自动重试耗尽后的告警 | 查看技术日志与重试结果；不替代业务判断 |

### 5. 核心业务对象

| 对象 | 唯一性/关系 | 说明 |
|---|---|---|
| ADM 原始文件 | 一份或多份文件可关联同一解析记录 | 保存原文件和解析来源，不因字段修正覆盖原文件 |
| ADM 解析记录 | 每次接收形成一条；可关联既有业务单 | 承接自动解析与人工确认，不把技术失败独立状态化 |
| ADM 业务单 | ADM 单号规范键全局唯一对应一张 | 规范键去除 ASCII/全角空白并统一英文字母大小写；原始 ADM 单号文本仍在原文件/解析审计中保留；可包含多个 PNR、多个票号 |
| PNR/票号明细 | 一张业务单 0..N 条 | 保存解析值、查询结果和来源线索；不形成独立业务单状态机 |
| 内部核实记录 | 一张业务单 0..N 条 | 记录核实对象、问题、回复和材料 |
| 供应商申诉轮次 | 一张业务单 0..N 轮 | 每轮提交、补件、反馈均为不可覆盖的事实记录；业务单只展示当前进度与最新结果摘要 |
| OTA 责任核实 | 一张业务单一份当前结论 | 待核实/已核实为核实进度；责任结果为 OTA 责任/非 OTA 责任 |
| OTA 追偿记录 | OTA 责任时一张业务单仅一条“当前关系”记录；责任修正可保留多条历史记录 | 记录可多轮补件、提交和反馈；外部提交由人工执行；历史已完成记录不因责任修正改写结果 |
| 供应商内部账单 | 一张业务单 0..N 张 | 位于既有供应商内部账单模块，业务类型=ADM；创建、调整、作废等均复用既有通用能力 |
| OTA 通用账单 | 一条追偿记录 0..1 张 | 追偿成功或部分成功且金额明确时按既有能力生成/关联 |
| 操作记录 | 各业务对象 0..N 条 | 保存状态/字段/金额变更前后值、操作人、时间、原因和系统事件 |

### 6. 核心业务流程

#### 主流程

##### 1. 接收并建立唯一业务对象

1. 接收 ADM 文件并保存原文件、来源和接收信息。
2. 【子流程1：解析并确认 ADM 标准字段】。
3. 生成 ADM 单号规范键并查询现有 ADM 业务单。
4. 【判断】是否已存在相同规范键的业务单
   - 是：将本次解析记录关联到已有业务单，不创建第二张业务单。
   - 否：按【子流程1】的确认方式生成唯一 ADM 业务单：全部必需字段均由系统硬性规则唯一确定且校验通过时由系统确认，否则必须由人工二次确认。

##### 2. 系统先完成订单/来源关联并准备核实证据

1. ADM 业务单生成后，系统立即使用有效票号、PNR、OID 等已取得信息查询 OMS/直连等内部系统；该查询不以 ADM 事件已经人工核实为前提。
2. 【判断】是否已可靠查询并核验到一个或多个内部订单
   - 是：自动保存具体订单号和目标核验结果，关联状态进入“已关联内部订单”。
   - 否：按适用条件调用【子流程2：识别四家授权 OTA 的 PNR 操作来源】。
3. 【判断】四家授权 OTA 来源是否形成确定性结果
   - 是：自动保存具体外部来源与目标核验结果，关联状态进入“已关联外部来源”；该结果仍只表示来源，不代表 OTA 责任。
   - 否：关联状态保持“待确认”，完整展示已查询结果、失败/冲突原因和人工补充入口。
4. 系统自动查询完成后，ADM 处理员可直接查看或修正关联结果；人工修正关联同样不要求先形成事件结论。
5. 将原始 ADM、OID、内部订单查询、PNR/票号、GDS 来源线索和异常说明统一作为下一阶段人工核实证据。

##### 3. 基于关联证据核实 ADM 事件、场景和金额

1. 【人工】基于系统已准备的订单/来源关联、OID、原始原因、内部操作和业务说明，判断 ADM 事件本身是否成立。
2. 【判断】事件核实结果
   - 成立：允许选择/修正 ADM 类型，并继续场景与金额核实。
   - 不成立：进入【子流程3：人工提交并跟进供应商申诉】，不得直接结束或生成账单；已确认的订单/来源关联作为证据保留。
   - 仍无法确认：向出票、退票或商务发起业务补充；回复后返回本阶段继续核实；已取得的关联证据不清空。
3. 根据 ADM 类型、所属业务环节和现有证据展示高频/高风险核实卡。
4. 【人工】确认 ADM 类型、当前金额和供应商费用；未知值不得以 0 代替。
5. 【判断】是否需要供应商确认、减免或补证
   - 是：进入【子流程3：人工提交并跟进供应商申诉】。
   - 否：继续责任与账单处理。

##### 4. 核实 OTA 责任并衔接追偿

1. 展示订单/来源关联、PNR 来源线索、内部操作和已有材料。
2. 【人工】确认 OTA 责任结果、具体责任 OTA、原因和依据。
3. 【判断】责任结果
   - OTA 责任：创建 OTA 追偿记录，初始状态为“待材料补充”；外部追偿仍由人工提交。
   - 非 OTA 责任：不创建追偿记录。
4. 【判断】责任是否仍未核实，或 OTA 责任但追偿记录尚未创建成功
   - 是：阻断有效 ADM 业务单“处理完成”，保留明确待办；不阻断已经满足条件的供应商内部账单生成。
   - 否：继续完成校验。

##### 5. 生成供应商内部账单并完成 ADM 主流程

1. 【判断】事件、关联、类型、当前金额和供应商申诉是否满足内部账单生成条件
   - 否：保持对应待办继续处理。
   - 是：在既有供应商内部账单模块生成业务类型为 ADM 的账单。
2. 【判断】账单生成结果
   - 成功：关联账单号并执行完成校验。
   - 失败：保持“待生成内部账单”，自动重试；仍失败时通知人工并触发技术告警。
3. 【判断】是否满足有效 ADM 业务单完成条件
   - 是：进入“处理完成”；OTA 追偿后续在独立记录中继续推进。
   - 否：回到“待 ADM 核实”，展示首个未满足条件，不删除已生成账单。

#### 子流程1：解析并确认 ADM 标准字段

1. 识别 BSP、ARC 或其他格式，解析公共字段、完整原因原文、附件、全部 PNR/票号和金额构成。
2. 【判断】自动解析是否形成可用结果
   - 是：标记每个字段的解析方式、规则编号、来源位置和校验结果。
   - 否：保持解析记录“待人工确认”，自动重试并提供人工补录；不新增技术失败状态。
3. 【判断】生成/关联业务单的全部必需字段是否均由系统硬性规则唯一确定，且不存在 AI 候选、冲突、缺失或人工修改
   - 是：执行字段完整性、币种字典、ADM 单号规范键唯一性和并发校验；通过后由系统自动确认并生成/关联业务单。
   - 否：保持“待人工确认”，展示原文件、字段来源和待处理原因，由 ADM 处理员二次确认。
4. 【人工】补充或修正当前字段；币种只能从“汇率管理”去重字典加 CNY 中选择。
5. 【判断】生成业务单核心字段是否完整且通过校验
   - 是：人工确认后输出标准字段，并生成/关联唯一业务单。
   - 否：保持“待人工确认”，列出待处理原因。

#### 子流程2：识别四家授权 OTA 的 PNR 操作来源

1. 【判断】当前 PNR 是否已确认属于四家授权 OTA 直接操作 PNR 的特殊分支
   - 否：不执行本子流程，不因缺少标识推断为淘宝。
   - 是：提取 PNR 信息。
2. 【判断】标识识别结果
   - 存在携程标识：来源线索为携程。
   - 存在去哪儿标识：来源线索为去哪儿。
   - 存在同程标识：来源线索为同程。
   - 三家标识均不存在：来源线索为淘宝。
3. 【循环】逐个处理业务单内全部 PNR。
4. 【直到】全部 PNR 已处理。
5. 【判断】是否每个 PNR 均进入特殊分支、识别成功且来源一致
   - 是：按 A-008 形成确定性外部来源关联候选/结果；OTA 责任仍需人工核实。
   - 否：关联状态保持或退回“待确认”，清除陈旧外部来源和责任结论。

#### 子流程3：人工提交并跟进供应商申诉

1. 【人工】整理申诉原因、当前申诉金额和材料。
2. 【人工】通过既有邮件能力、供应商网站或其他渠道完成外部提交。
3. 【判断】外部提交是否实际成功
   - 是：进入“待供应商反馈”，新增不可覆盖的提交轮次。
   - 否：保持“待提交供应商申诉”；自动重试适用的技术调用，并允许改用人工渠道。
4. 【判断】供应商反馈类型
   - 要求补件：回到“待提交供应商申诉”，补齐后再次人工提交。
   - 已形成最终反馈：【人工】记录申诉结果和当前金额，返回 ADM 核实或账单生成阶段。
   - 暂无反馈：保持“待供应商反馈”。

## 二、详细需求

## 模块 1：ADM 解析记录管理

### 1. 需求简介

- **目的：** 将 BSP、ARC 等格式差异较大的原始 ADM 转换为可核实的标准字段，并确保 ADM 单号不会生成重复业务单。
- **触发：** 用户上传文件、系统接收文件或人工新建解析记录。
- **输出：** 仍待人工确认，或经系统自动确认/人工确认后已结束处理并形成“生成新业务单 / 关联已有业务单 / 文件无需处理”之一的处理结果。

### 2. 业务文字流程图

#### 主流程

##### 1. 建立解析记录并保留原始材料

1. 【人工】选择一个或多个 ADM 文件并确认上传。
2. 【判断】单个文件是否上传成功
   - 是：保存原文件、来源渠道、接收时间和文件哈希，为该文件生成“待人工确认”解析记录。
   - 否：在上传环节展示失败原因和重试入口，不生成空解析记录；同批其他成功文件继续处理。

##### 2. 自动解析并归一化

1. 识别 BSP、ARC 或其他来源格式。
2. 解析 ADM 单号、航司/供应商、代理/IATA、发行日期、币种、票据金额、余额、原因、相关单据、全部 PNR/票号、金额构成和附件清单。
3. PNR 和票号按数组保存；原因文本中的 PNRRef、附件或结构化表格中的候选值均参与提取，不得只保留第一项。
4. 币种候选须匹配直连系统“汇率管理”返回的币种去重列表，列表固定额外补充 CNY（人民币）；不得接受任意三位文本作为合法币种。
5. 【判断】自动解析是否完整且一致
   - 是：标记每个字段的来源类型（系统硬性规则/非硬规则候选/人工）、规则编号、来源位置和校验结果。
   - 否：记录待处理原因，保持“待人工确认”；可自动重试，重试不改变业务状态。

##### 3. 分流系统确认与人工确认

1. 生成 ADM 单号规范键：去除 ASCII/全角空白并统一英文字母大小写；原始文本继续保留。
2. 【判断】生成/关联业务单的全部必需字段是否均由系统硬性规则唯一确定，且无 AI 候选、冲突、缺失、低置信或人工修改
   - 是：进入系统确认分支；系统执行核心字段、币种字典、规范键唯一性与并发创建校验。
   - 否：保持“待人工确认”；ADM 处理员对照原文件补充、修正并二次确认当前标准字段。
3. 【判断】系统确认分支的全部校验是否通过
   - 是：系统记录确认方式=系统确认、规则清单和确认时间；继续最终唯一性判断。
   - 否：不得降级为静默默认；转“待人工确认”，明确缺失、冲突或校验原因。
4. 【判断】人工确认分支的核心字段是否完整且已由当前操作人确认
   - 否：保持“待人工确认”；未知币种和金额不得默认填值。
   - 是：记录确认方式=人工确认、确认人和确认时间；再次执行规范键唯一性与并发创建校验。
5. 【判断】最终唯一性校验是否通过
   - 已出现已有业务单：关联已有业务单，不创建第二张。
   - 未出现已有业务单：生成一张 ADM 业务单并进入“待 ADM 核实”；解析记录进入“处理结束”，处理结果为“生成新业务单”。
6. 系统确认与人工确认共用同一幂等生成/关联服务；处理结束后均保留确认方式、规则/操作人和完整操作记录。

### 3. BSP/ARC 归一化规则

| 标准字段 | BSP 样例来源 | ARC 样例来源 | 规则 |
|---|---|---|---|
| ADM 单号 | Document Number / ADM Number | Memo | 必填；原始文本保留，唯一性规范键去除 ASCII/全角空白并统一英文字母大小写 |
| 外部状态 | Status / Issued | Status | 仅保留原渠道值，不直接映射为内部业务状态 |
| 航司/供应商 | Airline Code / Name | Carrier / Supplier | 同时保存代码和名称；缺一不补造 |
| 代理/IATA | Agent Code / Name | Agency | 保存代码、名称及原始文本 |
| 发行日期 | Issue Date | Issued | 使用日期控件；保留原时区/原文本供核对 |
| 币种 | Currency | Original Amount/Balance 币种 | 候选值必须匹配“汇率管理”币种去重列表，列表额外补充 CNY；金额字段必须同时有币种 |
| 票据金额 | Amount / Difference Total | Original Amount | 作为该语义字段当前值；人工修正时写操作记录 |
| 当前余额 | 若文件提供则提取 | Balance | 非所有格式必有；不把缺失默认成 0 |
| 原因 | Issue Reason / Reason and Sub Reason | Reason Category / Reason Note | 保存结构化分类和完整原文 |
| PNR | 原文/附件中候选值 | PNRRef | 支持多值；无法确认的候选标记“待人工确认” |
| 票号/相关单据 | Related Document Number | Ticket # | 支持多值；真实文件注明虚拟票号时保留说明，不以票号强关联内部订单 |
| 金额构成 | Fare、Tax、Fees 等 | Fare、Tax、Service Charge 等 | 按原格式映射到可扩展明细；公共金额只取当前值 |
| 附件 | Airline attachments | Attachments | 保存文件名、来源和关联，不解析失败即丢弃 |

### 4. 解析记录字段

| 字段            | 类型    | 必填/默认          | 说明                                    |
| ------------- | ----- | -------------- | ------------------------------------- |
| 解析记录号         | 编码    | 系统生成           | 解析对象唯一标识                              |
| 原始文件          | 附件    | 必填             | 保留原文件，不覆盖                             |
| 来源格式          | 枚举    | 系统识别/人工修正      | BSP、ARC、其他                            |
| ADM 单号原始/展示值  | 文本    | 生成业务单前必填       | 原文件文本和人工确认显示值；不得因规范化丢失原文              |
| ADM 单号规范键     | 文本    | 系统生成           | 去除 ASCII/全角空白并统一英文字母大小写；业务单全局唯一索引键    |
| 外部状态          | 文本    | 非必填            | 原渠道状态，不作为内部状态                         |
| 航司/供应商代码与名称   | 文本    | 生成业务单前必填       | 至少明确三位航司代码；按文件实际内容保存                  |
| 代理/IATA 代码与名称 | 文本    | 生成业务单前必填       | 至少明确代理/IATA 标识；用于后续 OID 核实            |
| 发行日期          | 日期    | 生成业务单前必填       | 默认取解析值，人工确认                           |
| 币种            | 币种枚举  | 生成业务单前必填       | 枚举直读“汇率管理”币种并去重，再额外补充 CNY；未知不得默认或自由输入 |
| 票据金额          | 金额    | 生成业务单前必填       | 当前最新解析/人工确认值；未知不得填 0                  |
| 当前余额          | 金额    | 非必填            | 文件明确提供时保存                             |
| ADM 原因分类      | 枚举/文本 | 非必填            | 结构化候选，最终类型由人工确认                       |
| ADM 原因原文      | 长文本   | 非必填            | 保留完整外部文本                              |
| PNR 列表        | 多值文本  | 与票号列表至少一项必填    | 0..N 个，允许候选确认                         |
| 票号/相关单据列表     | 多值文本  | 与 PNR 列表至少一项必填 | 0..N 个，可标记虚拟票号                        |
| 金额构成          | 结构化明细 | 非必填            | 仅保存外部文件明确出现的项                         |
| 附件清单          | 附件列表  | 非必填            | 保存外部附带材料                              |
| 待处理原因         | 多选枚举  | 状态=待人工确认时必填    | 字段缺失、候选冲突、疑似重复、自动解析未完成等；原因不是状态        |
| 字段确认方式/依据     | 枚举+明细 | 处理结束时必填        | 系统确认/人工确认；系统确认保存硬规则编号清单，人工确认保存确认人和时间  |
| 处理结果          | 枚举    | 状态=处理结束时必填     | 生成新业务单、关联已有业务单、文件无需处理；属于结果字段，不作为状态    |
| 关联业务单号        | 编码    | 生成/命中已有单后必填    | 关联唯一业务单                               |

### 5. 解析记录状态

#### 5.1 状态矩阵

| 从当前状态可流转到 → | 待人工确认（PAR-0） | 处理结束（PAR-1） |
|---|---|---|
| 待人工确认（PAR-0） | — | PAR-0→PAR-1：已经生成/关联业务单，或确认文件无需处理 |
| 处理结束（PAR-1） | PAR-1→PAR-0：授权重开并重新确认 | — |

#### 5.2 流转明细

| 流转                       | 必填内容                                                            | 校验                                                                            |
| ------------------------ | --------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| PAR-0→PAR-1（系统确认）        | 全部必需字段、字段硬规则编号清单、确认方式=系统确认、处理结果                                 | 所有必需字段均由系统硬性规则唯一确定；无 AI 候选/冲突/缺失/人工修改；币种在“汇率管理”去重列表+CNY 中；规范键唯一性、并发和业务单幂等校验通过 |
| PAR-0→PAR-1（人工确认生成新业务单）  | ADM 单号、航司代码、代理/IATA、发行日期、币种、票据金额、至少一个 PNR 或票号、人工确认人、处理结果=生成新业务单 | ADM 单号规范键不存在其他业务单；校验必须覆盖空白/大小写变体与并发创建；币种必须来自字典；不得默认币种或金额；业务单创建成功并已关联          |
| PAR-0→PAR-1（人工确认关联已有业务单） | 处理结果=关联已有业务单、已有业务单号、人工确认人                                       | ADM 单号已存在唯一业务单；不得创建第二张业务单                                                     |
| PAR-0→PAR-1（文件无需处理）      | 处理结果=文件无需处理、原因                                                  | 原因属于文件重复或误传；不得删除原始文件和操作记录                                                     |
| PAR-1→PAR-0（授权重开）        | 重开原因、操作人                                                        | 具备纠错权限；保留原处理结果与业务单关联历史，重新生成待办                                                 |

#### 5.3 状态定义

| 状态    | 是否终态 | 下一步                                                                             |
| ----- | ---- | ------------------------------------------------------------------------------- |
| 待人工确认 | 否    | 存在非硬规则候选、冲突、缺失、人工修改或校验未通过时，由 ADM 处理员核对、补录并二次确认；全字段硬规则记录由系统在同一状态内完成校验并直接流转，不长期停留 |
| 处理结束  | 是    | 当前无后续动作；根据处理结果查看新业务单、已有业务单或无需处理原因；确需纠错时由授权人员重开                                  |

### 6. 交互要求

- 列表页采用传统经典结构，不与详情并排：页面顶部为搜索区，页面下方为数据区。
- 搜索区默认展示 ADM 单号/解析记录号、来源格式、解析状态、航司/代理、发行日期区间；待处理原因放入“更多筛选”。提供“查询、重置、展开/收起”，重置后恢复默认条件。
- 数据区使用表格，至少展示解析记录号、ADM 单号、来源格式、航司/代理、PNR/票号数量、票据金额、解析状态、待处理原因、更新时间和操作；表格底部提供分页，总数与当前页范围可见。
- 行操作“处理/查看”进入独立详情页；返回列表时保留搜索条件、页码、排序和原行定位。
- 详情采用左侧原文件/原文、右侧标准字段的对照布局；多 PNR/票号使用可增删标签或明细表，不使用逗号长文本。
- 标准字段区展示“确认方式”。全字段硬规则且校验通过的记录由系统自动确认并直接生成/关联业务单；只要存在一个非硬规则候选、冲突、缺失或人工修正，即显示人工确认入口。
- 币种使用下拉枚举，只展示“汇率管理”去重结果并补充 CNY；不得提供自由文本兜底。字典读取失败时保持“待人工确认”，自动重试并通知人工/技术，不新增异常状态。
- “确认并生成业务单”为高风险主按钮，点击后先执行唯一性校验。
- 已处理记录提供“授权重开”入口；重开不删除已生成业务单，也不自动回滚下游，而是进入纠错与联动失效流程。每次解析字段确认/修正记录字段前后值、操作人和时间。
- 自动解析过程中可显示临时加载反馈，但“解析中/解析失败”不持久化为业务状态。
- 解析服务重试耗尽时，在当前页面展示可理解的失败原因、人工补录入口，并通知技术；其他解析记录仍可正常处理。
- 仅供原型评审的“为何不增加失败状态、为何使用当前值”等说明使用 `PrototypeNote`，不作为业务页面常驻文案。

## 模块 2：ADM 业务单管理

### 1. 需求简介

- **目的：** 在一张唯一 ADM 业务单内完成事件、归属、金额、供应商申诉、OTA 责任和供应商内部账单闭环。
- **默认负责人：** ADM 处理员。
- **前置条件：** 解析记录已生成业务单；一张业务单可含多个 PNR/票号。一期整单只选择一种最终关联类别，可在该类别下关联一个或多个明确的内部订单号/外部 OTA，不做明细级拆责或拆金额。
- **完成结果：** 有效业务单至少已生成一张既有供应商内部账单且无账单差异待处理，关联目标已核验、OTA 责任已人工核实；若为 OTA 责任，当前关系的独立追偿记录已成功创建。

### 2. 业务文字流程图

#### 主流程

##### 1. 订单/来源自动关联与证据准备

1. ADM 业务单生成后，系统立即使用有效票号或 PNR 在 OMS/直连等内部系统查询订单；同时使用 OID 等已取得信息补充归属证据。该动作不等待人工核实 ADM 事件。
2. 【判断】是否查到并核验通过一个或多个内部订单
   - 是：系统自动将关联状态更新为“已关联内部订单”，保存有效内部订单号列表与目标核验结果；人工打开业务单时直接看到结果，不要求重复确认系统已可靠掌握的信息。
   - 否：继续判断是否适用四家授权 OTA 特殊分支。
3. 【判断】是否已经确认属于“四家授权 OTA 通过 GDS 直接操作 PNR”的特殊分支（提取pnr信息的接口，提取不成功，调用恢复编码接口，再次提取pnr）
   - 否：保持“待确认”，保留内部查询结果并展示人工补充入口；不得进入下述淘宝兜底判断。
   - 是：逐个 PNR 自动执行来源识别：
   - 有携程标识 → 携程；
   - 有去哪儿标识 → 去哪儿；
   - 有同程标识 → 同程；
   - 三家标识均不存在 → 淘宝。
4. 上述“无三家标识即淘宝”只在已确认进入特殊分支后成立。其他 OTA 不具备直接操作 PNR 权限，不进入此判断，也不得因无标识被推断为淘宝。
5. 【判断】所有 PNR 是否均识别成功且来源线索一致
   - 是：系统自动将关联状态更新为“已关联外部来源”，保存具体 OTA 和目标核验结果；仅形成来源事实，不形成 OTA 责任结论。
   - 否：保持/退回“待确认”，展示逐项未识别/冲突原因；如原有关联已因新的 PNR/票号证据失效，则清空陈旧目标并使旧 OTA 责任结论失效。
6. 系统自动查询完成后，ADM 处理员可以在同一证据板块补充或修正关联：同一关联类别下可有多个具体内部订单号或多个具体 OTA；内部与外部类别不得同时作为最终关联；“其他 OTA”必须填写具体名称并通过 OTA 字典/主数据核验。
7. 人工修正关联不以“事件成立”为前置条件。关联类别、至少一个具体目标及目标有效性明确时保存结果；仍无法明确时保持“待确认”，但已取得的查询证据继续供事件核实使用。
8. 解析字段、PNR/票号或查询结果变化导致原关联错误时，退回“待确认”并执行关联下游结论失效规则；单纯修改 ADM 事件结论不清空仍有效的订单/来源关联。

##### 2. 基于系统关联证据核实 ADM 事件

1. 新业务单状态仍为“待 ADM 核实”；系统完成首轮订单/来源查询后，当前待办根据结果显示“确认关联异常”或“核实 ADM 事件”。
2. ADM 处理员直接基于原文件、OID、系统已查询的内部订单、PNR/票号、GDS 来源线索、内部操作和业务说明核实事件，不需要先自行跨系统重新查找。
3. 【判断】ADM 事件是否已经核实成立
   - 是：允许 ADM 处理员选择/修正 ADM 类型，并继续场景、金额和后续责任处理。
   - 否，已确认不成立：真实 ADM 已经存在，进入“待提交供应商申诉”向供应商提出异议；不得以“无需继续处理”代替申诉；已确认关联事实保留。
   - 尚不能判断：发起出票/退票/商务补充，状态进入“待业务补充”；补充完成后回到本阶段重新核实；已取得关联证据保留。
4. 示例：OID 并非我方、退票按规则只能退税但是否确实多退尚未确认，均由人工结合系统提前准备的关联与操作证据形成结论。
5. ADM 类型属于事件成立后的业务分类；事件为“待核实”或“不成立”时，页面不开放类型选择/修改，不得通过先选类型形成下游场景、金额或责任结论。

##### 3. 高频与高风险场景核实

系统根据人工确认的 ADM 类型展示核实卡。核实卡只组织证据与动作，不产生独立状态或自动责任结论。

| 优先级 | 场景与月报证据 | 主要数据来源 | 核实卡与高频动作 | 缺失/冲突兜底 | 保存与审计 |
|---|---|---|---|---|---|
| P0 | 重复预订/Churning：四个月共 991 单；真实样本集中出现多 PNR、无/虚拟票号和原因长文本 | 原 ADM 原文/附件、全部 PNR、GDS 历史、OMS/直连查询、授权 OTA 标识 | 多 PNR 集中展示取消次数、航段、乘客、原始原因；批量查询内部订单和四家授权 OTA 来源；直达关联与责任核实入口 | 任一 PNR 分支未确认、未识别或来源冲突均保持待确认并人工处理；虚拟票号不强关联订单 | 逐 PNR 保存查询时间、来源线索与原始证据；人工关联/责任结论分别留痕 |
| P0 | 航段取消率：11 月至 1 月月报报告 30、359、359 单 | 原 ADM、航司/航线、采购地、预订/取消统计、店铺设置和业务说明 | 集中展示航司、航线、采购地、取消率计算依据、当前限制/设置；支持按相同组合批量定位和发起业务补充 | 原始明细或计算口径不完整时不自动形成事件/责任结论；12 月与 1 月相同数值在目标度量前需核验 | 保存统计区间、口径、查询摘要和人工核实结论 |
| P0 | 无效航段未擦除：12 月至 2 月至少 469 单，11 月亦被列为增幅来源 | 原 ADM、GDS 航段历史、出票/退票记录、航变记录、清段操作记录 | 集中展示 HX、UN、TK 等状态、出现时间、是否出票/航变和已有清段记录；一键发起出票/退票补充 | 数据缺失或时序冲突时保持事件待核实，不按状态码自动判事件或责任 | 保存本次查询摘要、人工核实说明、业务补充问答与附件 |
| P0 | 售后退票/改签：四个月售后问题共 801 单；月报反复出现税费多退、退款罚金、改签未同步 TC 码 | 原 ADM、关联退票/改签单、退款前后金额、税费/罚金明细、TC 码、操作人与时间 | 同屏对比航司计算、系统计算、实际退款/改签与操作记录；支持一键向售后补充核实 | 属于部门维度，不直接改写 ADM 类型；规则或税种不明确时保留人工判断，不自动判责 | 保存关联售后单、金额构成、规则依据、业务回复和人工事件结论 |
| P1 | 更正票价：2 月 31 单、金额 3.8W | 原 ADM 金额构成、出票价、保证运价/政策配置、航司差额与服务费 | 并列展示出票价、适用政策、航司差额、航司自身费用构成、当前确认 ADM 和供应商附加服务费；突出缺失依据 | 配置或参数缺失时显示待人工确认，不自动填 0；需异议时进入供应商申诉 | 保存各业务语义的当前值；每次金额修改保存前后值、操作人、时间和原因 |
| P2 | 返点：四个月共报告 132 单 | 原 ADM、政策来源、返点/TC 码、系统与人工操作说明 | 复用商务补充与通用核实卡 | 不自动认定供应、系统或出票员责任 | 保存政策依据与补充记录 |
| P2 | 虚假预订：2 月 18 单、金额 0.8W | 原 ADM、PNR 创建来源、历史操作和原因原文 | 复用通用事件、关联、金额与责任核实 | 一期不新增拦截规则或独立处置状态 | 保存来源线索与人工结论 |

##### 4. 金额核实

1. 系统展示原文件金额、当前余额、当前规则计算值、航司原始费用构成和当前确认 ADM 金额。它们是不同业务语义，不是同一字段的历史快照。
2. 有已确认配置时，系统执行确定性计算；配置或参数缺失时显示“待人工确认”，不得静默填 0。
3. ADM 处理员确认当前 ADM 金额、供应商 ADM 服务费、供应商申诉服务费及可回收金额。航司在原 ADM 内列示的 Service Charge/Fee 只属于“航司原始费用构成”，如果已包含在当前确认 ADM 金额中，不得再次计入供应商 ADM 服务费。
4. 若事件或金额需要供应商确认、减免或补证，进入“待提交供应商申诉”。
5. 未发生供应商申诉时，供应商申诉服务费当前值明确为 0；发生申诉但费用未知时不得填 0。
6. 供应商应付合计 = 当前确认 ADM 金额 + 供应商 ADM 服务费当前应付 + 供应商申诉服务费当前应付。可回收金额不冲减供应商应付合计。
7. 当前确认 ADM、两类服务费当前应付、两类服务费当前可回收共五项当前值必须经同一金额更新服务写入。页面人工修改、解析纠错、供应商最终反馈和其他上游入口不得绕过该服务；每次写入记录五项前后值，并按需联动 OTA 当前拟追偿金额。
8. 【判断】事件、场景与五项金额是否均已明确
   - 是：继续判断是否需要供应商申诉。
   - 否：保持当前处理状态并明确下一项待补充内容，不得以默认 0 或空值绕过。

##### 5. 供应商申诉

1. 【判断】是否需要向供应商申诉或补证
   - 是：进入“待提交供应商申诉”，下一步由 ADM 处理员整理材料并人工提交。
   - 否：记录“无需申诉”及依据，继续 OTA 责任核实/账单前校验。
2. 页面提供“使用邮件模板”入口，可勾选多个既有供应商邮件模板、预览合成内容并调用既有发件能力；系统不建设收件/回复会话。通过网站或其他渠道提交时人工记录渠道和时间。
3. 【判断】是否已经实际提交成功
   - 是：进入“待供应商反馈”，新建一条不可覆盖的供应商申诉轮次。
   - 否：保持“待提交供应商申诉”；发送或提交技术失败不改变状态，自动重试后仍失败时保留人工替代渠道和技术告警。
4. 【判断】供应商反馈是否为最终结果
   - 否，要求补件：回到“待提交供应商申诉”；不新增“补件中”状态。
   - 是：人工记录结果字段“申诉成功/部分成功/申诉失败”；反馈中的当前金额调用统一金额更新服务，并联动仍为当前关系的 OTA 追偿记录。业务状态回到“待 ADM 核实”复核，或在其他账单条件已满足时进入“待生成内部账单”。
5. 结果字段用于历史和报表，不作为可流转状态。
6. 补件、反馈和再次提交追加到相应申诉轮次。业务单的“当前申诉进度/最新申诉结果”从轮次记录派生，不覆盖历史轮次。
7. 存在已提交但尚未收到最终反馈的申诉轮次时，业务单不得进入“待生成内部账单”或“处理完成”。

##### 6. OTA 责任核实与追偿衔接

1. 系统展示订单/来源关联、PNR 来源识别、内部操作和现有材料，只作为责任核实证据。
2. ADM 处理员人工确认责任结果：OTA 责任 / 非 OTA 责任，并填写责任原因和必要依据；依据作为业务单当前字段展示并随追偿记录带入，不得只写一条不可见日志。
3. 责任核实进度为“待核实/已核实”；责任结果是独立业务字段。
4. 【判断】OTA 责任是否已人工核实
   - 否：有效 ADM 业务单不得“处理完成”；供应商账单条件已满足时可先生成供应商内部账单。
   - 是，非 OTA 责任：不创建追偿记录，继续账单/完成校验。
   - 是，OTA 责任：系统自动创建一条 OTA 追偿记录，初始状态“待材料补充”；外部追偿不自动提交。
6. 创建追偿记录失败时：
   - 自动重试；
   - 仍失败则责任结论保留、业务状态不变，通知 ADM 处理员并触发技术告警；
   - 追偿记录创建成功前不得将有效业务单标记为“处理完成”，但不阻断已经满足条件的供应商内部账单生成。
7. 责任 OTA 不限于四家；选择“其他 OTA”时必须填写具体名称。责任结果或责任 OTA 修正后，原非终态追偿进入“无需继续处理”；原已完成追偿保留终态与结果、仅解除当前责任关系；新责任 OTA 新建“待材料补充”记录。

##### 7. 生成供应商内部账单并完成

1. 进入“待生成内部账单”前必须满足：
   - ADM 事件已人工核实为成立；
   - 订单/来源关联不是“待确认”，且具体目标有效性核验已通过；
   - ADM 类型已明确；
   - 当前确认 ADM 金额及供应商费用均明确；
   - 供应商申诉必要性已明确为“无需申诉”或申诉已结束；不存在“待判断”或未结束的申诉轮次；如发生过申诉，最新最终结果及当前金额已确认。
2. 【判断】账单前置条件是否全部满足
   - 是：系统在既有供应商内部账单模块生成账单，业务类型=ADM，并保存关联账单号列表；既有模块可按通用规则形成 0..N 张账单，ADM 业务单不复制创建、调整、作废逻辑。
   - 否：保持在对应处理状态并显示缺失项，不允许进入“待生成内部账单”。
3. 【判断】账单是否生成成功
   - 是：重新执行完成校验。
   - 否：保持“待生成内部账单”，自动重试；仍失败则通知人工和技术，不新增技术状态。
4. 【判断】有效业务单完成条件是否全部满足
   - OTA 责任已核实，且非 OTA 责任：进入“处理完成”；
   - OTA 责任已核实且追偿记录已创建：进入“处理完成”；
   - 责任未核实或 OTA 追偿记录尚未创建：回到“待 ADM 核实”，下一步明确为“核实 OTA 责任”或“等待追偿记录创建”，已生成账单保持关联。
5. 账单生成后不允许在 ADM 业务单直接修改对应已入账金额；新增、调整、冲销或差异处理均按既有账单通用流程执行，并把新账单/调整单继续关联回业务单。
6. OTA 追偿可以在 ADM 业务单完成后继续，不重新阻塞 ADM 主流程。
7. 已生成供应商内部账单后，如解析字段、ADM 类型、事件、最终关联或其他会影响账单有效性的上游结论被授权修正，旧账单不得删除，但业务单标记“账单差异待处理”并阻断“处理完成”。只有在既有通用账单流程确认原账单仍有效、完成调整/冲销或建立正确账单，并回写处理凭证后，才可解除阻断。

##### 8. 无需继续处理边界

- 可用于内部业务对象重复、误建、文件误传且已明确规范对象等系统对象级结束场景。
- ADM 事件核实为不成立时，必须先进入供应商申诉/补证，不能直接以“无需继续处理”跳过外部处理。
- 只有供应商已最终确认撤销、当前确认金额为 0 且无后续申诉、账单或其他业务动作时，ADM 处理员才可确认进入“无需继续处理”。未取得最终确认、申诉失败、仅部分减免或仍有金额争议时均不得结束。
- 必须填写原因；重复时必须关联规范 ADM 业务单；事件不成立结束时必须关联供应商最终反馈和金额归零依据。

### 3. 关键规则

| 编号 | 适用条件 | 规则/动作 | 业务结果 |
|---|---|---|---|
| BR-001 | 生成业务单 | ADM 单号按去除 ASCII/全角空白并统一大小写后的规范键全局唯一；原始文本保留 | 同一 ADM 单号及其显示变体永远只有一张业务单 |
| BR-002 | 一张 ADM 含多个 PNR/票号 | 全量保存并在同一业务单展示 | 不因明细数量拆成多张业务单 |
| BR-003 | 业务单生成后 | 系统先执行订单/来源查询与确定性关联；人工基于关联证据核实事件 | 关联查询不等待事件结论；事件成立后才进入类型、最终金额、责任和账单路径；不成立仅进入供应商申诉/补证，满足撤销归零条件后方可结束 |
| BR-003A | 选择或修改 ADM 类型 | 事件必须已人工核实为成立 | 事件待核实/不成立时类型入口禁用，先回到事件核实 |
| BR-004 | 最终关联 | 只允许待确认/已关联内部订单/已关联外部来源；最终态必须带目标有效性核验结果 | 内部必须有经 OMS/直连核验的订单号；外部必须有具体且经 OTA 主数据/授权来源证据核验的 OTA |
| BR-005 | 四家授权 OTA 特殊分支 | 仅在进入授权分支后按携程/去哪儿/同程标识识别，三者均无则淘宝 | 其他 OTA 永不进入该逻辑 |
| BR-006 | 系统识别 PNR 来源 | 仅标记来源线索 | 不自动生成 OTA 责任结论 |
| BR-007 | 有效业务单完成前 | OTA 责任必须人工核实 | 未核实不阻断已满足条件的供应商账单生成，但不得处理完成 |
| BR-008 | OTA 责任成立 | 自动创建追偿记录，状态=待材料补充 | 外部提交仍由人工执行 |
| BR-009 | 金额字段被修改 | 仅覆盖当前值；写入前值、后值、操作人、时间和必要原因 | 可追溯但不生成金额快照 |
| BR-010 | 技术调用失败 | 自动重试；失败后保留原状态并通知/告警 | 不增加技术异常状态，不丢业务待办 |
| BR-011 | 生成供应商内部账单 | 写入既有模块且业务类型=ADM | 后续走统一通用流程 |
| BR-012 | 高频场景 | 展示场景核实卡与快捷动作 | 共享主状态机，不按 ADM 类型拆状态 |
| BR-013 | 上游事实/关联/责任被修正 | 按依赖关系使下游结论失效并生成明确待办 | 不让旧责任、旧追偿或旧金额静默沿用 |
| BR-014 | 生成供应商内部账单 | ADM 类型必须明确，且无未结束申诉轮次 | “待选择”不是合法类型，不能绕过账单校验 |
| BR-015 | 上游当前金额从任一入口变化 | 必须调用统一金额更新与追偿联动；五项当前值前后均留痕 | 不允许供应商反馈等入口绕过追偿同步和金额审计 |
| BR-016 | 已有供应商账单后修正上游结论 | 保留旧账单历史并置“账单差异待处理”；通用账单流程回写凭证前阻断完成 | 防止仅凭仍有账单号而错误完成业务单 |

### 4. 字段说明

#### 4.1 基础与关联字段

| 字段 | 类型 | 必填/默认 | 规则 |
|---|---|---|---|
| ADM 业务单号 | 编码 | 系统生成 | 内部单号 |
| ADM 单号 | 文本 + 规范键 | 必填 | 展示值来源于解析确认；唯一性按规范键校验并建立全局唯一索引 |
| 业务状态 | 枚举 | 默认待 ADM 核实 | 见状态定义 |
| 当前待办 | 系统派生文本 | 必填 | 展示当前首个未完成动作，不作为独立状态 |
| ADM 类型 | 枚举 | 事件核实后必填 | 读取现有 ADM 类型配置；本期重点示例包括重复预订、航段取消率、无效航段未擦除、更正票价、返点、虚假预订、退款/佣金/其他问题，不以本文示例替代完整配置 |
| 所属业务环节 | 枚举/系统映射 | 非必填 | 售前、售后、其他；用于场景卡和统计，不与 ADM 类型混为同一维度 |
| 事件核实结果 | 枚举 | 进入后续处理前必填 | 待核实、成立、不成立 |
| 事件核实说明 | 长文本 | 结论形成时必填 | 说明 OID、业务操作或规则依据 |
| 关联状态 | 枚举 | 默认待确认 | 只允许三种值 |
| 关联目标核验结果 | 布尔+来源+时间 | 最终关联时必填 | 记录 OMS/直连、OTA 主数据或四家授权来源证据的成功核验；未通过时不得进入最终关联 |
| 内部订单号列表 | 关联编码列表 | 条件必填 | 关联状态=已关联内部订单时至少一项，且每项有效 |
| 外部来源 OTA 列表 | 枚举/关联列表 | 条件必填 | 关联状态=已关联外部来源时至少一项，且每项具体明确；“其他 OTA”必须落具体名称 |
| PNR 列表 | 明细列表 | 非必填 | 支持多值、查询结果和来源线索 |
| 票号列表 | 明细列表 | 非必填 | 支持多值和虚拟票号标记 |
| 来源识别分支 | 枚举 | 系统判断 | 未进入/四家授权 OTA；其他 OTA 不进入 |
| PNR 来源识别结果 | 明细字段 | 非必填 | 携程/去哪儿/同程/淘宝/未识别/冲突；仅证据 |

#### 4.2 金额字段

| 字段 | 类型 | 必填/默认 | 规则 |
|---|---|---|---|
| 原文件票据金额 | 金额+币种 | 条件必填 | 当前解析/人工确认值；与原文件可对照 |
| 原文件当前余额 | 金额+币种 | 非必填 | ARC 等格式明确提供时保存 |
| 航司原始费用构成 | 金额明细+币种 | 非必填 | Fare、Tax、Service Charge/Fee 等原文件构成；已包含在原文件/当前确认 ADM 时不重复加总 |
| 当前规则计算金额 | 金额+币种 | 条件必填 | 最新确定性计算值；参数缺失不填 0 |
| 当前确认 ADM 金额 | 金额+币种 | 生成账单前必填 | 当前最终承担值，不保留版本快照 |
| 供应商 ADM 服务费当前应付 | 金额+币种 | 生成账单前必填 | 未知不得填 0 |
| 供应商 ADM 服务费当前可回收 | 金额+币种 | 默认 0 | 不计入供应商应付合计 |
| 供应商申诉服务费当前应付 | 金额+币种 | 未申诉默认 0；已申诉则必填 | 已申诉但未知不得填 0 |
| 供应商申诉服务费当前可回收 | 金额+币种 | 默认 0 | 不计入供应商应付合计 |
| 供应商应付合计 | 金额+币种 | 系统计算 | 当前确认 ADM 金额 + 两类当前应付服务费 |
| 金额修改原因 | 长文本 | 条件必填 | 人工修改关键金额且变更有业务影响时必填 |

#### 4.3 申诉、责任与账单字段

| 字段 | 类型 | 必填/默认 | 规则 |
|---|---|---|---|
| 供应商申诉结果 | 枚举 | 发生申诉并收到最终反馈时必填 | 申诉成功、部分成功、申诉失败；不是状态 |
| 当前供应商申诉进度 | 系统派生 | 默认无需申诉/待判断 | 无需申诉、待提交、待反馈、已结束；从不可覆盖的申诉轮次派生，不作为业务结果 |
| 供应商申诉金额 | 金额+币种 | 每次提交时必填 | 每轮提交记录内保存当次值；业务单展示最新一轮摘要 |
| 提交渠道 | 枚举/文本 | 每次提交时必填 | 邮件、供应商网站、其他 |
| 提交时间 | 日期时间 | 每次提交时必填 | 默认当前时间，可按权限修正 |
| 供应商反馈说明 | 长文本 | 收到反馈时必填 | 保存原结果摘要及附件 |
| 供应商反馈日期 | 日期 | 收到反馈时必填 | 默认当前日期，按实际反馈日期保存并进入轮次审计 |
| OTA 责任核实进度 | 枚举 | 默认待核实 | 待核实、已核实 |
| OTA 责任结果 | 枚举 | 已核实时必填 | OTA 责任、非 OTA 责任；不是状态 |
| 责任 OTA | 枚举/关联 | OTA 责任时必填 | 不限于四家；四家规则只用于 PNR 来源特殊分支 |
| 责任原因与依据 | 长文本/附件 | 已核实时必填 | 系统线索 + 人工判断依据 |
| OTA 追偿记录号 | 关联编码 | OTA 责任时必填 | 系统创建成功后写入 |
| 供应商内部账单号列表 | 关联编码列表 | 处理完成时至少一项有效 | 既有模块账单，业务类型=ADM；允许关联 0..N 张 |
| 账单差异复核标记 | 布尔+处理凭证 | 默认否；已有账单后上游修正时为是 | 为是时阻断完成；处理结论、通用账单凭证、操作人和时间必填后清除 |

### 5. 订单/来源关联状态

#### 5.1 状态矩阵

| 从当前状态可流转到 → | 待确认（R0） | 已关联内部订单（R1） | 已关联外部来源（R2） |
|---|---|---|---|
| 待确认（R0） | — | R0→R1：有效内部订单号已建立并核验关联 | R0→R2：具体 OTA 已明确并核验 |
| 已关联内部订单（R1） | R1→R0：原关联需修正 | — | — |
| 已关联外部来源（R2） | R2→R0：原关联需修正 | — | — |

#### 5.2 流转明细

| 流转 | 必填内容 | 校验 |
|---|---|---|
| R0→R1 | 至少一个内部订单号、目标核验结果 | 每个订单存在且关联成功；清空外部来源 OTA 列表 |
| R0→R2 | 至少一个具体外部来源 OTA、目标核验结果 | 每个 OTA 明确且有效；“其他 OTA”已落具体名称；清空内部订单号列表 |
| R1→R0 | 修改原因 | 保留原订单号变更记录；重新生成待办 |
| R2→R0 | 修改原因 | 保留原 OTA 变更记录；重新生成待办 |

#### 5.3 状态定义

| 状态 | 是否终态 | 含义/下一步 |
|---|---|---|
| 待确认 | 否 | ADM 处理员继续确认关联类别及至少一个具体目标；不得长期停留，且未完成时阻断业务单完成 |
| 已关联内部订单 | 是 | 已明确一个或多个具体内部订单号 |
| 已关联外部来源 | 是 | 已明确一个或多个具体 OTA |

### 6. ADM 业务单状态

> 状态只表示当前处理进度、责任角色和下一步动作。事件成立与否、申诉成功与否、OTA 是否负责等均为结果字段。

#### 6.1 状态矩阵

| 从当前状态可流转到 →  | 待 ADM处理员 核实（A）                                 | 待业务补充（B）               | 待提交供应商申诉（C）              | 待供应商反馈（D）   | 待生成内部账单（E）                 | 处理完成（F）                         | 无需继续处理（G）                                            |
| --------------------- | ------------------------------------------------------ | ----------------------------- | ---------------------------------- | ------------------- | ----------------------------------- | ------------------------------------- | ------------------------------------------------------------ |
| 待 ADM 核实（A）      | —                                                      | A→B：事实/政策/操作需业务补充 | A→C：事件或金额需向供应商异议/补证 | —                   | A→E：事件成立且供应商账单条件满足   | A→F：已有有效供应商账单且完成校验满足 | A→G：对象重复/误建/误传，或事件不成立且供应商已撤销、金额归零、无后续动作 |
| 待业务补充（B）       | B→A：从核实发起且业务已回复                            | —                             | —                                  | —                   | —                                   | —                                     | —                                                            |
| 待提交供应商申诉（C） | C→A：提交前出现新事实需复核                            | —                             | —                                  | C→D：人工已实际提交 | —                                   | —                                     | —                                                            |
| 待供应商反馈（D）     | D→A：已收到最终/关键反馈，需复核                       | —                             | D→C：供应商要求补件/再次提交       | —                   | D→E：反馈已确认且供应商账单条件齐备 | —                                     | —                                                            |
| 待生成内部账单（E）   | E→A：账单已生成但仍需责任核实/追偿创建，或入账前被修正 | —                             | —                                  | —                   | —                                   | E→F：账单生成且完成校验满足           | —                                                            |
| 处理完成（F）         | F→A：授权纠错重开                                      | —                             | —                                  | —                   | —                                   | —                                     | —                                                            |
| 无需继续处理（G）     | G→A：授权纠错重开                                      | —                             | —                                  | —                   | —                                   | —                                     | —                                                            |

#### 6.2 流转明细

| 流转                     | 必填内容                                                 | 校验条件                                                     |
| ------------------------ | -------------------------------------------------------- | ------------------------------------------------------------ |
| 待 ADM 核实A→B待业务补充 | 核实对象、核实问题、期望材料；截止提醒可选               | 内部核实记录创建成功                                         |
| A→C                      | 申诉原因、申诉金额候选、所需材料；截止时间按已有配置展示 | 真实 ADM 已存在且需要供应商异议/补证；事件不成立不能直接结束 |
| A→E                      | ADM 类型、五项当前金额                                   | ADM 事件已人工核实为成立；关联非待确认且目标核验通过；五项金额明确；申诉必要性已判断且无未结束供应商申诉轮次；不要求 OTA 责任已核实 |
| A→F                      | 无新增人工字段                                           | 已有至少一张有效供应商内部账单；不存在“账单差异待处理”；责任已核实；OTA 责任时当前追偿记录存在 |
| A→G                      | 无需处理原因、规范业务单号（重复时）                     | 对象重复/误建/误传，或事件已人工核实为不成立且供应商已确认撤销、当前确认金额为 0、无后续申诉与账单动作；未取得供应商最终确认或仍有金额争议时不得结束 |
| B→A / B→C                | 业务结论、回复日期、说明、必要附件                       | 被分配角色已回复；按发起补充前的合法状态返回，记录保留       |
| C→A                      | 复核说明                                                 | 新事实使原提交方案失效                                       |
| C→B                      | 核实对象、问题、所需材料                                 | 当前提交材料依赖内部业务补充                                 |
| C→D                      | 实际提交渠道、时间、金额、原因、材料清单                 | 人工已完成外部提交；仅点击发送但失败不允许流转               |
| D→A                      | 供应商反馈说明、附件、结果候选                           | 已收到足以改变事实/金额的反馈                                |
| D→C                      | 补件要求、需补材料                                       | 供应商明确要求补件或再次提交                                 |
| D→E                      | 供应商申诉结果、当前确认 ADM 金额、费用字段              | 当前轮次已最终结束；其余供应商账单校验与 A→E 相同            |
| E→A                      | 系统生成结果或修正原因                                   | 账单生成成功但责任/追偿完成条件未满足，或账单生成前结论/金额被修正 |
| E→F                      | 无人工填写                                               | 至少一张既有供应商内部账单有效且业务类型=ADM；不存在“账单差异待处理”；责任已核实；OTA 责任时当前追偿记录存在 |
| F→A / G→A                | 重开原因、操作人、权限依据                               | 仅授权纠错；保留原终态、结果、外部提交和账单历史；如解析记录存在待同步纠错，重开后应用并执行下游失效/账单差异规则 |

#### 6.3 状态定义

| 状态 | 是否终态 | 当前角色与下一步 |
|---|---|---|
| 待 ADM 处理元核实 | 否 | ADM 处理员完成页面高亮的首个未完成任务：先处理系统关联仍未解决的异常，再核实事件、场景证据、金额或 OTA 责任 |
| 待业务补充 | 否 | 指定出票/退票/商务补充事实或材料 |
| 待提交供应商申诉 | 否 | ADM 处理员整理并人工提交申诉/补件 |
| 待供应商反馈 | 否 | 等待外部供应商回复；ADM 处理员收到回复后登记 |
| 待生成内部账单 | 否 | 系统生成既有供应商内部账单；失败时留在此状态并重试/告警 |
| 处理完成 | 是 | ADM 主流程完成；至少一张有效供应商账单已生成且无差异待处理，责任已核实，OTA 责任时当前追偿记录已创建；追偿后续可独立继续。确需纠错时由授权人员重开 |
| 无需继续处理 | 是 | 内部对象重复/误建/误传，或事件不成立且供应商已确认撤销、金额归零，已明确无后续业务动作。确需纠错时由授权人员重开 |
| 供应反馈异常<br />商务升级中 |  |  |



### 7. 操作记录

| 事件 | 必须记录 |
|---|---|
| 状态流转 | 当前状态、目标状态、操作人、时间、说明 |
| 关联修正 | 关联状态前后值、订单/OTA 前后值、操作人、时间、原因 |
| 事件/责任/申诉结果修改 | 结果前后值、操作人、时间、必要原因和依据 |
| 金额修改 | 五项当前金额各自的字段名、修改前值、修改后值、币种、操作人、时间、必要原因；不得只记录当前确认 ADM 和两类应付服务费 |
| 自动解析/查询/创建/账单失败 | 调用对象、失败摘要、重试次数/结果、通知结果；不改变业务状态 |
| 外部提交 | 渠道、时间、提交人、金额、材料清单、成功/失败结果 |
| 账单生成 | 账单号、业务类型、金额摘要、生成时间、失败/成功结果 |

#### 7.1 纠错与下游结论失效

| 被修正内容 | 必须失效/重算的下游内容 | 状态与后续动作 |
|---|---|---|
| 解析字段修正并同步到业务单 | 受影响的事件、关联、金额或责任结论按字段依赖标记失效 | 非终态回到“待 ADM 核实”；终态需授权重开后应用待同步纠错。已生成账单不删除，并置“账单差异待处理” |
| 事件结果或关键事件依据修正 | 当前金额、OTA 责任等依赖事件结论的内容需重新复核；已经独立核验有效的订单/来源关联继续保留，除非 PNR/票号/关联证据本身同时变化 | 回到“待 ADM 核实”或“待提交供应商申诉”；已有账单时置“账单差异待处理” |
| 关联类别或具体目标修正 | OTA 责任进度重置为待核实；原责任依据失效 | 回到“待 ADM 核实”；已有账单时置“账单差异待处理”；如已有当前追偿记录，按其是否终态执行下述规则 |
| OTA 责任由 OTA 改为非 OTA | 原当前关系的追偿记录不得继续代表新结论 | 非终态追偿进入“无需继续处理”；已完成追偿保留原状态/结果，仅标记“不再是当前责任关系”；业务单保留新责任结论 |
| 责任 OTA 改为另一 OTA | 原 OTA 追偿不得改名复用 | 原追偿非终态时进入“无需继续处理”；已终态时保留原状态/结果并解除当前关系；为新 OTA 自动创建新的“待材料补充”记录 |
| 当前金额在 OTA 外部提交前修正 | 当前拟追偿金额同步为最新值并留痕 | 追偿仍在“待材料补充”时保持；已到“待提交追偿”时退回“待材料补充”，增加“更新金额说明”并要求人工重新确认材料与金额 |
| 当前金额在 OTA 已提交后修正 | 不覆盖操作记录中该轮已发生的外部提交动作及其金额；当前拟追偿金额更新并标记需人工复核 | 追偿回到与外部反馈相符的待办：需要补交时进入“待材料补充”，否则保持当前状态并提示差异 |

- 纠错不删除历史操作记录。已经实际发生的供应商申诉、OTA 追偿提交动作、追偿终态结果和已经生成的账单均保留原事实；通过“当前关系”与“账单差异待处理”标记表达新旧关系，不为金额类业务字段建立版本快照。
- 解析记录重开只是纠错入口；同步下游前必须展示影响对象并二次确认。

### 8. 页面与交互

#### 8.1 列表页

- 列表页采用传统经典结构，不与详情并排：页面顶部为独立搜索区，页面下方为表格数据区。
- 搜索区默认展示 ADM 单号/业务单号、ADM 类型、业务状态、关联状态、责任进度、航司和日期区间；外部来源 OTA、责任结果、当前待办、所属业务环节、供应商账单是否生成放入“更多筛选”。提供“查询、重置、展开/收起”，重置后恢复默认条件。
- 搜索区可提供快捷场景：重复预订、航段取消率、无效航段未擦除、售后退票/改签、更正票价、返点、虚假预订；快捷场景仅写入筛选条件，不创建新状态。
- 数据区使用表格，默认展示 ADM 单号、业务单号、类型、PNR/票号数量、当前状态、当前待办、关联结果、责任进度、当前确认金额、更新时间和操作；表格底部提供分页，总数与当前页范围可见。
- 行操作“处理/查看”进入独立详情页；返回列表时保留搜索条件、页码、排序和原行定位。
- “待确认”关联和临近外部截止的记录应有明确视觉提示；具体超时阈值依赖现有时效配置，不在本期文档编造数值。

#### 8.2 详情页

从上到下按处理顺序呈现：

1. 顶部摘要：ADM 单号、类型、状态、当前待办、币种/当前确认金额、负责人。
2. 处理进度：系统关联 → 事件核实 → 金额核实 → 供应商处理 → OTA 责任 → 内部账单。进度项展示结果摘要但不伪装成状态。
3. **订单 / 来源与 OTA 责任（合并板块）：** 在同一板块连续展示原始 PNR/票号、系统自动查询到的关联 PNR/票号/内部订单、授权 OTA 来源线索、关联状态与目标核验结果，以及人工 OTA 责任进度/结果/依据和追偿记录入口。系统关联与来源线索在事件核实前即可展示和修正；OTA 责任按钮仅在事件成立且责任核实条件满足后开放。来源事实与责任结论视觉分区、字段分离，不因合并板块而合并业务语义。
4. ADM 事件与基础信息：展示事件核实结果、航司/OID、核实说明和外部原因；人工基于上一板块已准备的关联证据进行核实。
5. 多 PNR/票号的逐项查询过程在合并板块内可展开查看；不再单独重复建设来源线索板块。
6. 高频场景核实卡：按 ADM 类型动态展示，默认展开 P0/P1 场景的关键证据。
7. 当前动作区：只展示完成下一步真正需要的按钮和字段；使用统一“进行处理”状态流转弹窗。
8. 金额区：展示不同语义的当前值；修改时使用确认弹窗并要求必要原因。
9. 供应商内部账单：显示“既有供应商内部账单 / 业务类型 ADM”，不展示独立 ADM 账单状态机。
10. 操作记录：按时间倒序，支持查看字段前后值和系统重试。
11. 跨模块定向跳转：从业务单跳追偿、从追偿跳业务单、从业务单回解析记录时，先清除会隐藏目标的冲突筛选，再定位明确目标；目标不存在时提示错误，不得静默打开列表第一条。

统一“进行处理”弹窗必须直接复用原型模板 `StateTransition`：当前状态只读；只显示按当前数据条件计算出的合法目标状态；合法目标作为可切换选项卡；切换目标后仅展示该次流转所需表单；提交后再执行服务端合法性校验。结果字段修改、金额修改、来源/责任核实等不直接改变状态的动作可以使用独立业务弹窗，但不得伪装成状态选项。

处于“待提交供应商申诉”时，详情提供“使用邮件模板”：支持多选既有模板、预览发送内容并调用现有发件能力。只有发件结果成功才创建申诉轮次并流转到“待供应商反馈”；发送失败保留当前状态、记录失败结果并执行重试/人工渠道/技术告警。

### 9. 接口与复用能力

| 交互方 | 方向 | 目的 | 关键输入/输出 | 失败处理 |
|---|---|---|---|---|
| IATA/OID 映射 | 输入 | 核实代理/OID | IATA→OID/归属线索 | 无法确认转人工，不生成技术状态 |
| OMS/直连 | 输入 | 关联并核验内部订单、取得业务信息 | 票号/PNR→有效订单号、关联结果、出票/退票信息 | 核验失败保持关联待确认，记录失败并人工继续 |
| OTA 主数据/字典 | 输入 | 核验人工选择的具体 OTA | OTA 标识/名称→有效主体 | 无法核验或只选“其他 OTA”时保持待确认，要求补充具体名称 |
| GDS | 输入 | 获取 PNR 历史和四家授权 OTA 来源线索 | PNR→历史/标识/失败原因 | 自动重试；仍失败保持原状态和关联待确认，通知人工/技术 |
| 汇率管理 | 输入 | 提供 ADM 币种枚举 | 有效币种列表→去重后补充 CNY | 自动重试；仍失败保持解析记录当前状态，禁止自由输入新币种并通知人工/技术 |
| ADM 配置 | 输入 | 类型、费用、时效的确定性计算 | 航司/类型/日期→当前适用规则 | 缺失时人工确认，不填 0 |
| 既有邮件发件能力 | 双向 | 发送供应商申诉/补件 | 模板+业务数据→发送结果 | 失败不流转，允许人工渠道并告警 |
| OTA 追偿记录管理 | 输出 | 确认 OTA 责任后创建追偿记录 | 业务单/OTA/金额/依据→待材料补充记录 | 重试；失败保持业务状态，可生成供应商账单但禁止业务单处理完成 |
| 既有供应商内部账单模块 | 输出 | 生成业务类型=ADM 的供应商内部账单 | 当前最终金额/费用→账单号 | 失败保持待生成内部账单并重试/告警 |
| 既有供应商外部账单模块 | 输入 | 关联外部 ADM 账单 | ADM 单号→业务单/内部账单 | 无 PDF/业务单时暂停通用对账并提示索取材料；不新建状态 |

## 模块 3：OTA 追偿记录管理

### 1. 需求简介

- **创建条件：** ADM 业务单人工核实结果=OTA 责任。
- **创建方式：** 系统自动创建并关联，初始状态必须为“待材料补充”。
- **处理边界：** 系统组织材料、记录动作和生成待办；ADM 处理员人工向 OTA 外部渠道提交。
- **完成结果：** 人工确认追偿结果；成功/部分成功时按实际追回金额生成或关联既有 OTA 通用账单。

### 2. 业务文字流程图

#### 主流程

##### 1. 自动建单并准备材料

1. 系统从 ADM 业务单带入 ADM 单号、责任 OTA、责任依据、当前确认金额、PNR/票号和原始附件，创建追偿记录并进入“待材料补充”。
2. ADM 处理员补充追偿材料。
3. 【判断】材料是否已经人工确认完整
   - 是：进入“待提交追偿”。
   - 否：保持“待材料补充”，展示仍缺材料和当前金额变化提示。

##### 2. 人工提交 OTA

1. ADM 处理员在 OTA 外部渠道完成人工提交，并记录渠道、时间、提交金额、材料和外部单号。
2. 【判断】是否已实际完成外部提交
   - 是：进入“待 OTA 反馈”，新增一条不可覆盖的外部提交操作记录。
   - 否：保持“待提交追偿”；技术失败只触发自动重试、人工渠道和技术告警。
3. 【判断】提交前是否发现材料缺失或上游当前金额变化
   - 是：退回“待材料补充”，补齐材料/更新金额说明并重新人工确认。
   - 否：继续等待 OTA 反馈。

##### 3. 跟进反馈并确认结果

1. 【判断】OTA 是否要求补件
   - 是：从“待 OTA 反馈”或“待结果确认”回到“待材料补充”；不得新建“补件中”或“提交失败”状态。
   - 否：收到可供判断的反馈后进入“待结果确认”。
2. 【判断】是否仍需再次沟通
   - 是：实际发送后回到“待 OTA 反馈”，新增再次沟通记录。
   - 否：ADM 处理员记录追偿结果、当前实际追回金额和依据。
3. 【判断】追偿结果
   - 追偿成功/部分成功：按当前实际追回金额生成或关联既有 OTA 通用账单；账单成功后进入“处理完成”。
   - 追偿失败：当前实际追回金额确认固定为 0，不生成 OTA 账单，进入“处理完成”；不得保存非零实际追回金额。

##### 4. 无需继续处理与审计

1. 【判断】追偿记录是否重复、误建，或上游责任是否经授权修正为非 OTA 责任
   - 是：进入“无需继续处理”，保留原因、历史外部动作和当前关系变化。
   - 否：按当前状态继续处理。
2. 每次人工外部提交、补件、反馈、再次提交和结果确认均追加审计记录。追偿记录首页只展示当前金额类业务字段与最新操作摘要；历史变更通过操作记录查询。

### 3. 字段说明

| 字段 | 类型 | 必填/默认 | 规则 |
|---|---|---|---|
| 追偿记录号 | 编码 | 系统生成 | 唯一标识 |
| ADM 业务单号/ADM 单号 | 关联/文本 | 必填 | 只读关联 |
| 责任 OTA | 枚举/关联 | 必填 | 来自人工责任结论 |
| 追偿状态 | 枚举 | 默认待材料补充 | 见状态定义 |
| 追偿结果 | 枚举 | 默认未形成 | 追偿成功、部分成功、追偿失败；不是状态 |
| 是否当前责任关系 | 布尔 | 默认是 | 责任修正后，历史已完成记录置否；不改写其原状态、结果或金额 |
| 被替代原因/新记录号 | 文本/关联 | 条件必填 | 当前关系解除时保存责任修正依据和新追偿记录号（如有） |
| 外部提交操作记录 | 明细列表 | 0..N 条 | 每次实际提交动作的金额、渠道、时间、材料、外部单号及后续反馈不可覆盖；属于操作审计，不是金额字段快照 |
| 当前拟追偿金额 | 金额+币种 | 提交前必填 | 只保存当前值；修改留操作记录 |
| 材料清单 | 附件/勾选项 | A→B 前必填 | 原 ADM、PNR/票号、责任依据及 OTA 要求材料 |
| 材料完整确认 | 布尔+操作人时间 | A→B 必填 | 必须由人工确认 |
| 提交渠道 | 枚举/文本 | B→C 必填 | OTA 平台、邮件、其他 |
| 外部追偿单号 | 文本 | 非必填 | 外部渠道生成时记录 |
| 提交时间 | 日期时间 | B→C 必填 | 默认当前时间 |
| OTA 反馈说明/附件 | 长文本/附件 | C→D 必填 | 保存外部反馈摘要 |
| OTA 反馈日期 | 日期 | C→D 必填 | 默认当前日期，按实际反馈日期保存；不得展示后忽略 |
| 当前实际追回金额 | 金额+币种 | 成功/部分成功时必填；失败固定 0 | 成功/部分成功必须大于 0；追偿失败时 0 是已确认业务结果而非未知默认值，非零禁止保存 |
| OTA 通用账单号 | 关联编码 | 成功/部分成功完成时必填 | 既有通用账单 |
| 无需处理原因 | 长文本 | 进入无需继续处理时必填 | 重复时必须引用同一业务单下的规范追偿记录；误建或责任修正保留说明 |

### 4. 状态矩阵

| 从当前状态可流转到 → | 待材料补充（T0） | 待提交追偿（T1） | 待 OTA 反馈（T2） | 待结果确认（T3） | 处理完成（T4） | 无需继续处理（T5） |
|---|---|---|---|---|---|---|
| 待材料补充（T0） | — | T0→T1：人工确认材料完整 | — | — | — | T0→T5：重复/误建/责任修正 |
| 待提交追偿（T1） | T1→T0：发现材料缺失 | — | T1→T2：人工已实际提交 | — | — | T1→T5：重复/误建/责任修正 |
| 待 OTA 反馈（T2） | T2→T0：OTA 要求补件 | — | — | T2→T3：已收到反馈 | — | T2→T5：重复/误建/责任修正 |
| 待结果确认（T3） | T3→T0：反馈要求再次补件 | — | T3→T2：已再次沟通并等待反馈 | — | T3→T4：结果及账单条件满足 | T3→T5：重复/误建/责任修正 |
| 处理完成（T4） | — | — | — | — | — | — |
| 无需继续处理（T5） | — | — | — | — | — | — |

### 5. 状态流转明细

| 流转 | 必填内容 | 校验条件 |
|---|---|---|
| T0→T1 | 材料清单、材料完整确认、当前拟追偿金额 | 人工确认材料完整；责任结果仍为 OTA 责任 |
| T1→T0 | 缺失材料说明 | 尚未外部提交 |
| T1→T2 | 渠道、提交时间、提交金额、材料清单、外部单号（如有） | 人工已实际完成外部提交；技术发送失败不允许流转 |
| T2→T0 | OTA 补件要求 | OTA 明确要求补件 |
| T2→T3 | OTA 反馈说明、附件 | 已收到可供人工判断的反馈 |
| T3→T0 | 再次补件要求 | 当前反馈要求补充材料 |
| T3→T2 | 再次提交/沟通记录 | 已实际发送，继续等待 OTA 反馈 |
| T3→T4 | 追偿结果；成功/部分成功时当前实际追回金额和 OTA 账单号 | 失败不生成账单；成功/部分成功时既有 OTA 通用账单生成/关联成功 |
| 任意非终态→T5 | 无需处理原因；重复时规范追偿记录号；责任修正依据（如适用） | 仅重复、误建或授权后的责任修正；不得覆盖外部提交历史 |

### 6. 状态定义

| 状态 | 是否终态 | 当前角色与下一步 |
|---|---|---|
| 待材料补充 | 否 | ADM 处理员补齐并人工确认 OTA 追偿材料；系统创建后的初始状态 |
| 待提交追偿 | 否 | ADM 处理员在 OTA 外部渠道完成人工提交 |
| 待 OTA 反馈 | 否 | 等待 OTA 回复；收到后由 ADM 处理员登记 |
| 已成功OTA处理中 |  | （更新ota的具体状态，外部状态，已审核，） |
| 待结果确认 | 否 | ADM 处理员确认追偿结果、实际金额并完成账单衔接 |
| 处理完成 | 是 | 结果已确认；需账单时既有 OTA 通用账单已生成/关联 |
| 无需继续处理 | 是 | 追偿记录重复、误建或责任修正，无后续追偿动作 |

> T4、T5 均为终态，不为责任修正改写状态。若 T4 已完成后上游责任变化，通过“是否当前责任关系=否”解除关联并保留历史；新责任 OTA 使用新追偿记录。该处理见 A-007/Q-007。

### 7. 页面与交互

- 列表页采用传统经典结构，不与详情并排：页面顶部为独立搜索区，页面下方为表格数据区。
- 搜索区默认展示追偿记录号/ADM 单号、追偿状态、责任 OTA、ADM 类型和创建日期区间；结果、提交时间、反馈时间、账单状态放入“更多筛选”。提供“查询、重置、展开/收起”。
- 数据区表格至少展示追偿记录号、ADM 单号、责任 OTA、追偿状态、当前下一步、当前拟追偿金额、最新提交时间、追偿结果、更新时间和操作；表格底部提供分页和总数。
- 行操作“处理/查看”进入独立详情页；返回列表时保留搜索条件、页码、排序和原行定位。
- 详情顶部突出“当前下一步”，结果只在摘要卡显示，不作为状态标签。
- 待材料补充页展示系统已带入材料和人工仍需补充项；材料未完整时禁用“确认材料完整”。
- 待提交追偿页提供复制摘要、下载材料包、“发现材料缺失”和“记录已提交”；不提供“系统自动提交”按钮。
- 待结果确认页提供“再次沟通并等待”入口；必须记录实际沟通渠道、时间和内容后才能回到“待 OTA 反馈”。
- 待结果确认页分开展示 OTA 原始反馈、人工结果、当前实际追回金额和 OTA 通用账单。
- 所有金额修改遵循当前值 + 操作记录；详情页仅展示当前值，历史在操作记录中查询。

### 8. 技术异常处理

| 场景 | 自动处理 | 业务状态 | 人工/技术兜底 |
|---|---|---|---|
| 创建追偿记录失败 | 自动重试 | ADM 业务单保持原状态 | 通知 ADM 处理员；技术告警；不阻断已满足条件的供应商账单生成，但创建成功前不得处理完成 |
| 材料包生成失败 | 自动重试 | 追偿记录保持待材料补充或待提交追偿 | 允许人工下载/整理单项材料；技术告警 |
| 外部提交工具失败 | 自动重试（如使用既有发件能力） | 保持待提交追偿 | 人工改用 OTA 平台/其他渠道并记录 |
| OTA 通用账单生成失败 | 自动重试 | 保持待结果确认 | 通知账单角色和技术；成功前不处理完成 |

## 三、非功能需求

### 1. 数据一致性

- ADM 单号唯一性校验必须使用去除 ASCII/全角空白并统一英文字母大小写后的规范键，同时覆盖解析确认和并发创建；数据库建立规范键唯一索引，不能只做前端字符串全等比较。
- 关联状态、目标字段和目标核验结果必须原子校验：内部订单号列表/外部 OTA 列表不得同时作为最终目标，所选列表至少一项且均已核验有效；字面值“其他 OTA”不是有效目标。
- 四家授权 OTA 来源自动关联必须同时满足：每个 PNR 均确认属于授权分支、每个 PNR 均成功识别、全部来源一致；过滤掉未识别 PNR 后再判断一致属于错误实现。
- 责任结果=OTA 责任时，具体责任 OTA 与“是否当前责任关系=是”的追偿记录号必须同时存在才能处理完成；不得据此阻断供应商内部账单生成。已完成历史追偿被解除当前关系后不能继续充当新责任结论的完成凭证。
- 金额必须带币种；不同币种不在本期自动相加。
- 账单生成接口需以业务单、账单用途和幂等键防重；允许通用流程按真实业务形成 0..N 张供应商账单，但同一创建请求重试不得生成重复账单。
- 有效 ADM 业务单处理完成校验至少包含：ADM 事件已人工核实为成立、已核验关联目标、ADM 类型、五项当前金额已明确、供应商申诉必要性已判断且无未结束轮次、至少一张有效供应商内部账单、无账单差异待处理、OTA 责任已核实，以及 OTA 责任时当前追偿记录已创建。
- 五项当前金额必须通过统一更新入口原子写入并分别留痕；当前确认 ADM 变化时同步当前追偿拟追偿金额。追偿已到“待提交追偿”时必须退回“待材料补充”重新确认。
- ADM 事件核实为不成立时，只允许继续供应商申诉/补证；只有供应商已最终确认撤销、当前确认金额为 0 且无后续动作时，才能以“无需继续处理”结束，不得生成供应商内部账单或进入“处理完成”。

### 2. 审计与权限

- 状态、事件结果、关联、责任、金额、外部提交和账单生成必须留痕。
- 金额只保留当前值，但操作记录不可被普通业务角色删除或修改。
- 已生成供应商内部账单的金额字段在 ADM 页面只读。
- 责任修正、终态“无需继续处理”和已入账后差异处理使用既有授权机制；具体角色映射以现有权限配置为准。

### 3. 稳定性与可恢复性

- 自动调用应具备幂等与有限重试；重试次数由技术规范确定，不在 PRD 编造。
- 单条解析、查询或账单失败不得阻断其他 ADM 正常处理。
- 自动失败必须让业务知道“当前业务状态未变、下一步如何人工继续”，同时向技术提供可定位日志。

### 4. 可用性

- 日期使用日期/日期时间控件；枚举使用选择控件；多 PNR/票号使用标签或明细表。
- 高风险金额与责任确认采用二次确认，显示影响对象和当前值。
- 高频场景关键信息在首屏可见，不要求用户在原 PDF、GDS 和多个系统间反复查找后才能判断。
- 结果字段和状态标签采用不同视觉语义，避免评审和操作人员混淆。

## 四、目标验证与数据统计

### 1. 验证指标

| 指标 | 口径 | 目标方向 | 当前目标值 |
|---|---|---|---|
| 高频/高风险场景单均处理时长 | 从业务单生成到处理完成，按重复预订、航段取消率、无效航段未擦除、售后退票/改签和更正票价分别统计 | 下降 | 待上线前基线确认，不编造百分比 |
| 待确认关联滞留 | 当前关联状态=待确认的数量与停留时长分布 | 下降，且不存在被当作终态长期搁置 | 待现状基线与业务时效配置确认 |
| 重复业务单拦截 | 同一 ADM 单号被唯一性校验拦截的重复创建次数 | 应全部被拦截 | 业务规则型目标：不得生成第二张业务单 |
| 完成校验拦截 | 因事件、关联、ADM 类型、金额、未结束申诉、责任、追偿记录或有效供应商账单缺失而被阻断的完成操作 | 可观测且零绕过 | 业务规则型目标：不得绕过 |
| 自动字段采用率 | 解析字段无需人工修改即被确认的比例，按 BSP/ARC 和字段统计 | 提升 | 需真实样本基线后设值 |
| OTA 追偿材料返工 | 从待 OTA 反馈退回待材料补充的记录占比 | 下降 | 需上线基线后设值 |

### 2. 报表口径

- 进度统计使用业务状态：待提交供应商申诉、待供应商反馈等。
- 结果统计使用独立结果字段：申诉成功/部分成功/失败、追偿成功/部分成功/失败。
- 月报可按“状态 + 结果”组合复现管理视角，但不得反向改变业务状态设计。
- 场景、部门、航司、供应、OTA、金额和时间维度延续现有报表口径；具体数据源映射在技术设计阶段确认。

## 五、上线计划与依赖

### 1. 上线顺序

1. ADM 解析、唯一性与通用业务状态底座。
2. 重复预订、航段取消率、无效航段未擦除、售后退票/改签和更正票价场景核实卡。
3. OTA 责任核实与“待材料补充”追偿流程。
4. 既有供应商内部账单和 OTA 通用账单衔接。
5. 报表口径和上线后基线采集。

### 2. 上线前依赖

- 确认现有 IATA/OID、OMS/直连、GDS、邮件、供应商内部账单和 OTA 通用账单接口可用范围。
- 由技术确认如何可靠判断“已进入四家授权 OTA 的 GDS 直接操作 PNR 特殊分支”，以及三家标识字段位置；该前提未满足时不得启用“无三家标识即淘宝”。
- 确认 ADM 类型/费用/时效配置的现有菜单、适用优先级和历史数据口径。
- 以真实 BSP/ARC 样本完成字段映射和多 PNR/虚拟票号测试。
- 明确现有权限角色与金额、责任修正、账单生成动作的映射。

### 3. 发布策略

- 先以可回退方式启用解析和只读来源线索，再启用自动创建追偿与账单衔接。
- 新旧处理中的业务单不强行迁移为结果型状态；迁移规则按新状态“下一步动作”映射。
- 上线后优先观察 P0 高频场景和更正票价高金额场景的处理时长、待确认关联滞留和解析字段修改率，再决定后续自动化投入。

## 六、方案假设、待确认项与后续边界

### 1. 本版采用的方案假设

| 编号 | 假设 | 影响 |
|---|---|---|
| A-001 | 一期一张 ADM 业务单最终只选择一种关联类别；该类别下可记录一个或多个具体内部订单号/OTA，但仍整单处理、不做明细级拆责或拆金额 | 若真实业务必须同时存在内部与外部两类归属，需新增混合归属与金额分配方案；当前保持待确认 |
| A-002 | 已有业务可以判断一条 PNR 是否属于“四家授权 OTA 特殊分支” | 若系统无可靠前置标识，则只能人工启用该分支，不能按缺标识自动判淘宝 |
| A-003 | 技术异常频率暂无证据支持专门的“异常待处理”状态 | 一期保留原状态 + 通知/告警；上线数据证明高频且需统一队列时再评估统一异常状态 |
| A-004 | 供应商申诉和 OTA 追偿的外部渠道均允许人工记录提交结果 | 若某渠道无法取得成功凭证，需要在接口设计补充可接受的证明方式 |
| A-005 | 既有供应商内部账单与 OTA 通用账单支持业务类型/来源关联和幂等创建 | 若不支持，应优先扩展通用模块，而不是在 ADM 内复制账单流程 |
| A-006 | 事件核实为不成立后，供应商最终确认撤销、当前确认金额为 0 且无其他后续动作，可以“无需继续处理”结束 | 这是为避免无出口而采用的当前产品假设，不来自已确认业务事实；若业务仍要求财务、统计或责任动作，则需调整结束条件或增加对应待办，但不应改成结果型状态 |
| A-007 | 已完成 OTA 追偿发生责任修正时，不改写已完成记录的状态/结果，只将“是否当前责任关系”置否；新责任 OTA 新建追偿记录 | 这是为保留真实历史结果并避免终态逆向流转采用的产品假设；若现有追偿模块没有关系标记，需要扩展字段或关系表 |
| A-008 | 四家授权 OTA 特殊分支满足“每个 PNR 均进入授权分支、识别成功且来源一致”时，系统可直接形成“已关联外部来源”；责任仍必须人工核实 | 若业务要求来源关联也必须逐单人工确认，则自动动作应改为“推荐候选 + 人工确认”，但不影响来源与责任分离 |
| A-009 | 四个月月报中的 ADM 类型、部门、平台、航司和处理进度属于不同统计维度；本版分别用作场景方向与页面需求依据，不跨维度相加或据此宣称覆盖率 | 月报仅支持优先级排序；正式基线、覆盖率和收益目标须由原始明细按统一口径重算 |

### 2. 待确认项

| 编号 | 待确认内容 | 当前处理方式 | 是否阻断当前完整版方案 |
|---|---|---|---|
| Q-001 | 一张 ADM 是否存在必须同时跨“内部订单”和“外部来源”两类归属，并需要按 PNR/票号拆责或拆金额的真实案例 | 同一类别可多目标；跨类别冲突保持待确认，不自动拆单/拆金额 | 不阻断原型评审；开发前需确认 |
| Q-002 | 关联“待确认”具体内部处理时限和升级提醒阈值 | 先生成持续待办并阻断完成，不编造时长 | 不阻断流程；阻断提醒配置开发 |
| Q-003 | ADM 类型、费用和申诉时效的完整配置字段/优先级 | 原型展示现有配置结果和缺失人工兜底 | 不阻断通用流程；阻断自动计算细节 |
| Q-004 | 供应商申诉结果更新后是否存在自动同步外部余额的可靠接口 | 一期由人工确认当前金额 | 不阻断 |
| Q-005 | 真实 ADM 样本中附件压缩包、扫描件和多页表格的解析范围 | 一期保留原文件与人工补录 | 不阻断 |
| Q-006 | 事件不成立且供应商撤销、金额归零后，是否还存在必须完成的财务、责任或统计动作 | 当前按 A-006 进入“无需继续处理”，并保留供应商反馈和金额归零依据 | 不阻断原型评审；阻断开发阶段最终完成校验定稿 |
| Q-007 | 已完成追偿被责任修正后，现有追偿模块是否支持“解除当前关系但保留终态结果”，以及报表如何区分历史/当前关系 | 当前按 A-007 增加关系标记，历史结果不回写为“无需继续处理” | 不阻断原型评审；阻断追偿数据模型定稿 |
| Q-008 | 四家授权 OTA 确定性来源识别是否允许直接形成最终外部来源关联，还是必须人工二次确认 | 当前按 A-008 自动形成来源关联；任一 PNR 不满足即退回待确认并清除陈旧结论 | 不阻断原型评审；阻断自动关联上线策略 |
| Q-009 | OMS/直连与 OTA 主数据是否能返回可供最终关联使用的稳定有效性核验结果 | 原型以“目标已核验”模拟接口成功；真实接口不可用时保持待确认，不能靠任意文本完成 | 不阻断原型评审；阻断最终关联接口开发 |
| Q-010 | 2025-12 与 2026-01 月报中“航段取消率”均为 359 单是否来自同一原始口径，以及 2026-02 未单列的数量 | 本版仅将其作为高频方向性证据，不把 748 单作为正式上线基线或覆盖率分母 | 不阻断场景设计；阻断目标值定稿 |
| Q-011 | “汇率管理”币种接口的可用性、缓存时效和前端字典刷新机制 | 当前严格以接口去重结果+CNY 为合法枚举；接口失败时保持原状态并重试/告警，不允许自由输入绕过 | 不阻断原型评审；阻断解析确认可用性方案定稿 |

### 3. AC 衔接说明

当前处于产品设计与原型评审阶段，本次不新增或修改 AC。方案确认后，再依据本 PRD 的状态矩阵、完成校验、高频场景核实结果和异常恢复路径拆分可独立判定的 AC。

---

**评审结论摘要：** 本版以用户当前对话事实为最高业务依据；以 2025 年 11 月至 2026 年 2 月四份月报分别校准高频场景方向，并明确不同统计维度不得相加；以 BSP、ARC 真实文件校准字段、多 PNR/多票号、虚拟/无票号、不同币种、退票与佣金场景。任何系统来源识别都不替代人工责任核实，任何结果字段都不再承担业务状态职责。
